---
description: 项目计划定制、变更完整性、构建验证和交付证据；每轮对话适用
alwaysApply: true
---

# 项目变更与交付

## 计划定制

- 协议、设备 DLL 或测试判定改动须先提交计划，用户确认后再改代码。
- 实施中发现需要偏离已确认计划时，停止修改并重新确认。

## 变更完整性

- 修改前读取目标文件的当前内容，保留不属于本次任务的用户改动。
- 当前工程为非 SDK 风格项目；新增或删除 `.cs`、Designer、`.resx` 时同步维护 `.csproj` 中的 `Compile`、`DependentUpon`、`EmbeddedResource` 等项目项，不假定文件会自动纳入构建。

## 构建验证

- 使用 Visual Studio MSBuild 执行 `Debug|x86`；不以 `dotnet msbuild` 作为唯一验证。
- 仅修改 `AGENTS.md`、Rules、Skills 或说明文档时执行结构、引用和格式静态检查，不声称已完成项目构建。
- 修改源码、配置、资源或项目文件后执行实编，并记录实际命令、配置、输出目录和退出结果。
- 输出文件被占用时使用独立验证目录，不擅自结束用户程序或删除运行目录。
- 构建失败时区分编译错误、环境缺失和输出占用，不将环境问题描述为源码错误。

## 交付证据

- 改代码、配置、资源、项目文件或仓库规则后更新 `WJ_ComprehensiveTest_Desk_20260922/项目文件说明.md`。
- 未实编不得声称已构建通过。
- 交付列出修改文件、已验证、未验证、仍需实机确认项。
- 使用独立验证目录时在交付中列出实际输出路径。
