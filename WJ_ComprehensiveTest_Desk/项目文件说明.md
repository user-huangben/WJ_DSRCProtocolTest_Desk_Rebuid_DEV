# WJ_ComprehensiveTest_Desk 项目文件说明

2026-09-22 版本管理命名：exe 与主窗口标题为 `WJ_ComprehensiveTest_Desk_20260922`；解决方案、工程目录、`.csproj` 和文档路径为 `WJ_ComprehensiveTest_Desk`。C# 命名空间仍为 `WJ_DSRCProtocolTest_Desk_Net`。

2026-09-22 解决方案父目录由 `WJ_DSRCProtocolTest_Desk_Net` 更名为 `WJ_ComprehensiveTest_Desk_20260922`；根 `AGENTS.md`、Rules、Skills、`.gitignore` 路径已同步。C# 命名空间仍为 `WJ_DSRCProtocolTest_Desk_Net`。工作区磁盘目录 `WJ_DSRCProtocolTest_Desk_Rebuid_DEV` 需在 Cursor 外重命名。

2026-09-22 上位机与 Visual Studio 项目更名为 `WJ_ComprehensiveTest_Desk_20260922`：解决方案、源码目录、程序集、窗口标题和清单程序名已同步；C# 命名空间与 `RootNamespace` 仍为 `WJ_DSRCProtocolTest_Desk_Net`，未改源码编码。

2026-09-22 根 `AGENTS.md` 项目描述拆为技术栈、目录与分层、构建与验证、代码约定；代码约定仅保留窗体后置、`Services/`、`Native/` 落点，不重复 `csharp-dsrc-host.mdc` 的 Designer/事件、编码和 DSRC 判定。服务名单补入现行 `BeijingTypicalTransactionService`。项目边界与 Cursor 配置未改。

2026-09-22 建立 Git 基线：仓库根执行 `git init`；`.gitignore` 将受控 INI/DLL 例外路径改为现行 `WJ_DSRCProtocolTest_Desk_Net_20260920`，并忽略 `**/.backups/`。构建输出、测试日志/截图和编辑器临时文件继续忽略。未配置远程、未推送。

2026-09-22 `project.mdc` 全局规则扩充：在原计划定制、构建和交付要求上增加偏离确认、保留用户无关改动、非 SDK 风格 `.csproj` 项目项同步、纯指令/文档变更静态检查、实编证据记录、构建失败分类及独立输出路径报告。未重复根 `AGENTS.md` 的项目事实/边界/技术基线，也未重复 `csharp-dsrc-host.mdc` 的界面、编码和 DSRC 实现规则。

2026-09-21 C# Rule 内容分层重命名：`.cursor/rules/csharp-dsrc-host.mdc` 总标题改为「C# WinForms DSRC 开发规范」，正文按实际职责分为「界面分层与事件」「测试执行与结果」「文件与编码保护」「DSRC 记录与判定」；frontmatter 描述同步更新，glob、生效方式和规则条目未改。

2026-09-21 技术基线归并：将 .NET Framework 4.8、WinForms、x86 及代码/依赖兼容性约束从 `.cursor/rules/project.mdc` 迁入根 `AGENTS.md` 的项目边界；`project.mdc` 继续保留计划定制、构建验证和交付要求。

2026-09-21 技术基线表述精简：`.cursor/rules/project.mdc` 改为引用根 `AGENTS.md` 已声明的 .NET Framework 4.8、WinForms 和 x86 基线，Rule 仅保留不得擅自变更、代码与依赖兼容、禁止用改平台位数规避原生 DLL 错误等实施约束。

2026-09-21 项目 Rule 技术基线补充：`.cursor/rules/project.mdc` 将原「变更门禁」更名为「计划定制」，新增 .NET Framework 4.8、WinForms、x86 兼容性约束，以及 Visual Studio MSBuild、`Debug|x86` 和输出占用时使用独立验证目录的构建要求；未新增 Rule 文件。

2026-09-21 Rule 重复内容精简：根 `AGENTS.md` 继续作为项目边界和 Skill 路由的唯一来源；`.cursor/rules/project.mdc` 删除与其重复的范围、证据、受控资料、仓库操作、验证口径和 Skill 路由，仅保留协议/设备 DLL/测试判定改动的确认门禁及交付要求。源码专项 `csharp-dsrc-host.mdc` 未改。

2026-09-21 根 `AGENTS.md` 按三类重组：项目描述（概述、工程入口、架构地图）、项目边界、Cursor 配置。原「用户当前指令优先」和「C++/MFC 只读、备份目录不作实现来源」两句从描述挪入项目边界；约束含义、工程路径、Skill 路由未改。

2026-09-21 Cursor 指令正文格式标准化：删除内容已由根配置覆盖的 `WJ_DSRCProtocolTest_Desk_Net/AGENTS.md`；根 `AGENTS.md` 统一为项目概述、工程入口、架构地图、硬边界和 Cursor 配置结构，两个 `.mdc` Rule 统一规则分类，四个 `SKILL.md` 统一适用范围、输入/落点、工作流程、停止/交付和参考资料等正文层级。现有 frontmatter、约束含义、协议内容、触发条件和文件引用未改。

2026-09-21 Cursor Skill 目录精简：删除 developer 和 migration Skill 中仅供 OpenAI/Codex 兼容使用的 `agents/openai.yaml`；Cursor 所需的 `SKILL.md`、`references/` 和 `scripts/` 均保留，Skill 的发现与调用方式不变。

2026-09-20 Cursor Skill 内容审计与引用补齐：移除 `dsrc-csharp-host-developer/SKILL.md` 文末重复的第二段 frontmatter；从用户指定原始目录分别向 developer 和 migration Skill 的 `references/` 补入 DSRC 协议送检指南及用户卡/ESAM 文件结构 PDF。`operation-manual-docx` 补入自包含的 DOCX 生成验收规范，并在 `scripts/` 增加 `render_docx.py`、`mark_artifact_operation_started.mjs`，`SKILL.md` 同步改为明确引用 `build_manual.py`、渲染脚本和参考规范。`iteration-records` 审计后无需调整。

2026-09-20 操作说明 Skill 结构优化：将 `artifacts/manual/build_manual.py` 移至 `.cursor/skills/operation-manual-docx/scripts/build_manual.py`，脚本路径计算同步改为从工作区定位解决方案目录；`artifacts/manual/` 只保留原始截图、标注截图等生成过程产物。`operation-manual-docx/SKILL.md` 已同步说明脚本与产物的职责边界。

2026-09-20 Cursor Skill 预留结构：为 `.cursor/skills/` 下现有四个 Skill 分别增加由 `.gitkeep` 保留的 `references/` 和 `scripts/` 目录，便于后续按需存放详细资料和自动化脚本；已移除先前建立的空白 `reference.md`，现有 `SKILL.md` 内容及触发方式未修改。

2026-09-20 北京典型交易间隔运行配置修正：当前调试程序实际读取的 `bin/Debug/SetMe.ini [SET_Trade_GB]/interval` 为 `300 ms`，与原上位机 `Release/SetMe.ini` 的 `8000 ms` 不一致，导致六分支连续交易过快；现仅将运行目录间隔恢复为 `8000 ms`，交易次数和协议逻辑未改。进一步核对原 Release 可执行程序确认其构建时间为 2026-09-07，15:48 实机日志表现为两轮交易、每轮相隔约 8 秒，与当前提供的六分支源码并非同一构建行为；车辆校验密钥逻辑不能继续按该源码推断，尚未对此作无证据修改。

2026-09-20 北京典型交易第二轮 `93 02` 修正：`LaneTransactionService.Typical.cs` 的“更新0019+扣费”合并帧原先将扣费 APDU 的 4 字节终端交易序号留为 `00000000`，但 MAC1 过程密钥按旧软算固定序号 `000006C6` 计算，导致 OBU 在第二条扣费 APDU 返回 MAC 校验错误 `93 02`。现恢复旧 `ConsumeAndUpdata19_GB_onechip` 使用的 `000006C6`，使扣费帧字段与 MAC1 输入一致。已使用 Visual Studio MSBuild 18.9.1 将 `Debug|x86` 重新构建到 `bin/Debug_Verify/`，构建成功；仍需真实 OBU 复测第二轮扣费。

2026-09-20 北京典型交易 GetSecure 修正：`BeijingTypicalTransactionService` 不再复用普通车道交易中读取长度为 `0x4F` 的 GetSecure 模板，新增北京专用 26 字节请求并恢复旧 `WJ_OneChip_typicaltransfer` 的车辆信息读取长度 `0x20`、固定鉴别数据 `0102030401020304` 及算法尾字节。此项用于恢复后续 48/64 字节密文解密和车辆信息比较的原始输入条件；未修改通用车道交易 GetSecure。因正在调试的程序锁定 `bin/Debug`，常规输出复制失败；随后使用同一 Visual Studio MSBuild 18.9.1、`Debug|x86` 配置输出到 `bin/Debug_Verify/`，实编成功。

2026-09-20 北京地标典型交易迁移：`BeijingLocalStandardTestForm` 将 `OBU基本测试 > 3、典型交易测试` 登记为真实处理器，并增加默认勾选的“校验车辆信息”选项。新增 `Services/BeijingTypicalTransactionService.cs`，按旧 `WJ_OneChip_typicaltransfer` 固定执行六个分支；每个分支从当次 VST 独立取得算法和分散字段，金额读取 exe 同级 `SetMe.ini [TRADE_SET]/19` 前四字节、状态固定 `04`、间隔读取 `[SET_Trade_GB]/interval`。正常和失败清理均按单向 EventReport 处理，SetMMI 按旧北京语义只记录、不作为强制判据。`SoftTradeCryptoService` 增加 GetSecure 车辆信息的 3DES/SM4 解密校验，复用现有 one-chip 密钥依据；`北京地标协议测试用例迁移对照.md` 补充六分支矩阵与安全修正。已使用 Visual Studio MSBuild 18.9.1 实编 `Debug|x86` 成功；未连接真实 RSU/OBU，扣费、写0019/EF08、蜂鸣与最终通过仍需实机确认。

2026-09-20 操作说明素材目录：将工作区根目录 `artifacts/manual/` 移到解决方案内 `WJ_DSRCProtocolTest_Desk_Net/artifacts/manual/`（与 `.sln`、`操作说明.docx` 同级）。`build_manual.py` 的输出改为同级 `操作说明.docx`。未修改业务源码、DLL 或 INI。

2026-09-20 Cursor Agent 目录：根 `AGENTS.md` 改为当前 `WJ_DSRCProtocolTest_Desk_Net_20260920` 工程地图；工程约束拆到 `.cursor/rules/project.mdc`（alwaysApply）与 `.cursor/rules/csharp-dsrc-host.mdc`（现行项目 glob）。四个 skill 迁到 `.cursor/skills/`（开发、迁移、迭代记录、操作说明）。`.agents/skills/` 不再放置 `SKILL.md`。未修改业务源码、DLL 或 INI。

2026-09-16 空中帧流程结束规则：`.codex/rules/project.rules` 的 DSRC 专属区新增默认以单向 `EventReport` 数据帧作为空中帧流程结尾；仅用例明确要求后续帧、持续会话或其他收尾方式时例外。开发或移植时，如原测试用例和用户要求未明确结束后的处理，必须询问用户是否需要增加逻辑；未修改业务源码、协议流程、DLL、INI 或测试数据。

2026-09-16 常规 APDU 响应值与异常帧展示规则：`.codex/rules/project.rules` 的 DSRC 协议规则新增常规有应答 APDU 用例默认以 `90 00` 为成功状态字；刻意设计的异常状态、无响应或其他判据必须逐用例声明。常规用例收到非 `90 00`，例如交易中出现 `93 02` 时，测试信息展示框必须显示失败步骤、期望状态、实际状态和完整异常回复帧。`EventReport` 单向规则保持不变；未修改业务源码、协议流程、DLL、INI 或测试数据。

2026-09-16 交互控件实现顺序规则：`.codex/rules/project.rules` 在可复用 WinForms/C# 工程基线中新增“交互控件固定实现顺序”，明确 Designer 控件与布局、Designer 显式事件绑定、代码后置事件处理、服务调用入口和 Designer 双击跳转验证的五步顺序。未修改业务源码、窗体、资源、配置、DLL 或协议资料。

2026-09-16 Agent / Rules / Skill 分层重构：根目录 `AGENTS.md` 缩减为项目事实、受控输入、证据边界和交付授权范围；`.codex/rules/project.rules` 重组为可抽取给同类项目的 WinForms/C# 工程基线，以及仅适用于本项目的 DSRC 覆盖规则；`dsrc-csharp-host-developer` 和 `dsrc-csharp-host-migration` 两个 skill 删除重复的工程约束，只保留任务路由、开发/迁移流程和协议资料读取条件。删除无调用方且内容已并入 rules 的 `.agents/skills/dsrc-csharp-host-migration/references/shared-engineering-rules.md`；协议 PDF、DLL、INI、业务源码和窗体均未修改。

2026-09-16 三个标准测试窗体统一布局：`BeijingLocalStandardTestForm`、`WanjiCpcStandardTestForm` 和 `WatchmanBroadcastTestForm` 按 `.codex/rules/project.rules` 的“WinForms 测试大框界面与显示规则”统一为标题区、测试项选择区、中间操作区和右侧测试信息区；保留原有测试树、按钮事件、北京地标执行进度以及占位测试逻辑，三个日志区均增加自动滚动到最新信息。未修改协议服务、设备 DLL、INI 或测试用例逻辑。

2026-09-16 WinForms 测试大框界面规则纳入统一规则：将当前 OBU 标准测试窗体验证过的测试树、三栏布局、操作状态、测试信息滚动、流程说明、结果截图和 Designer/服务分层约束抽象为通用“WinForms 测试大框界面与显示规则”，加入 `.codex/rules/project.rules`。未修改业务源码、窗体 Designer、资源、配置或设备 DLL。

2026-09-16 项目规则合并：将 `.codex/rules/` 下原有的 `winforms.rules`、`protocol.rules`、`security-files.rules` 和 `validation.rules` 合并为统一的 `.codex/rules/project.rules`；保留根目录 `AGENTS.md` 作为项目总指令，两个项目级 DSRC skill 继续独立维护。未修改业务源码、配置、资源、协议资料或设备 DLL。

2026-09-14 255S 保持测试完整数据流修正（BID/UnixTime）：`WanjiStandardTestCaseHandlers.cs` 的用例入口不再弹出前置确认，启动后默认关闭 BID 与 UnixTime 自动更新并重新初始化台发，结束分别恢复原配置；从 `BST_Locked_Sutong.ini` 读取 BST/判定对并调用专用交易服务。这样可保持前五轮 BID 为 `0608FE01`，并原样发送配置时间：第 4 轮 `5F695FFF` 到第 5 轮 `5F6960FF` 固定增加 `0x100`（256）秒，第 5 轮相对首轮累计增加 556 秒；第 6 轮保持第 5 轮时间且仅将 BID 改为 `0608FE02`。`Services/DesktopCommService.cs` 提供只覆盖 BID/UnixTime 开关的安全重初始化入口，LLC 与 MAC 设置保持不变。`Services/LaneTransactionService.cs` 使用旧 `WJ_SutongWanjiTest_255_RuanSuan` 专用流程：判定 00 执行 GetSecure、ICC 0019/余额、消费初始化、软算 MAC1、扣费、非强制 SetMMI、EventReport 和 10 秒等待；判定 01 要求无 VST并额外等待 2 秒。金额和状态取 `SetMe.ini [TRADE_SET]/19`，算法跟随界面；交易序号从 00 开始按 0～254 循环，消费初始化模板时间字段保持旧函数固定的 `1A A1 DD 02`。`WanjiStandardTestForm.cs` 同步更新用例说明。

2026-09-14 拼帧轮次修正：`WanjiStandardTestCaseHandlers.cs` 移除原来仅顺序执行 GetSecure/TransferChannel 的伪拼帧步骤，新增与旧 `WJ_ProtocolTest_Link1` 一致的专用执行器。固定执行 1～5 帧五轮，按旧 k0～k4 顺序遍历每轮 `5^n` 个组合；五种类型为 GetSecure、IC TransferChannel、ESAM TransferChannel、SetMMI、Special。预计响应拼接长度超过 100 字节的组合跳过，各轮实际下发 5、25、125、612、2754 个组合；每轮开始建链一次，组合失败时 EventReport、等待 2 秒并重新建链，轮末 EventReport 并等待 3 秒。首帧类型决定调用对应 DLL 请求/响应函数，后续帧使用旧程序无公共 MAC/LLC 头的拼接片段。`WanjiStandardTestForm.cs` 同步修订双击说明，明确旧 Link1 的响应长度和逐字节比对代码处于注释状态，当前通过标准保持为请求/响应调用成功。

2026-09-14 OBU 信道选择至 255S 保持校对：`Services/DesktopCommService.cs` 增加保留既有初始化参数的物理信道重初始化入口；OBU 信道选择两个叶子用例会在发 BST 前切换到旧程序使用的 0/1 物理信道，并在结束后恢复测试前信道。`WanjiStandardTestCaseHandlers.cs` 的 GetSecure 四项按界面算法选择处理请求尾部算法能力字节，并严格按旧程序偏移校验认证器（3DES 为 100，SM4 为 108）。TransferChannel 12 个界面子项继续对应旧程序数组的 1～7 条 IC 同指令、1～3 条 ESAM 同指令以及两组 3 条不同指令；成功判定改为逐条解析 DataList，校验数量、每条 `90 00` 和最终 ReturnStatus `00`。

2026-09-14 备用车道交易金额与出入口状态读取修正：`Services/LaneTransactionService.cs` 修复简单 INI 解析器无法识别 `[TRADE_SET]//说明` 行内注释节标题的问题，使备用测试项“车道交易测试”能够实际读取 exe 同级 `SetMe.ini` 的 `[TRADE_SET]/19`。该值现在严格按 5 字节解释：前 4 字节作为扣费金额写入消费初始化并参与 MAC1，最后 1 字节作为出入口状态写入 ICC 0019；缺失、长度错误或非法十六进制字符会明确停止测试，不再静默使用全零参数。测试信息日志分别显示金额和出入口状态；`WanjiStandardTestForm.cs` 同步更新双击用例说明。当前配置 `0000001004` 对应金额 `00 00 00 10`、出入口状态 `04`。未修改 Designer、资源、INI、项目文件或 DLL。

2026-09-14 `AGENTS.md` 官方格式规范化：工作区根目录指令文件保持官方识别名称 `AGENTS.md`，一级标题调整为 `# AGENTS.md`，并补充其工作区适用范围说明。文件继续使用普通 Markdown，不添加 Codex 不要求的 YAML frontmatter；原有 DSRC、WinForms、DLL、协议、编码和验证规则保持不变。

2026-09-14 项目级 DSRC skill 初始化：将用户级 `dsrc-csharp-host-developer` 的有效内容纳入工作区 `.agents/skills/dsrc-csharp-host-developer/`，包含 `SKILL.md`、`agents/openai.yaml` 和两份被该 skill 按任务路由读取的协议 PDF。项目 skill 的正文和协议资料与用户级源文件保持一致；UI 元数据按原含义转换为 UTF-8 并显式保留自动触发策略。未复制两个历史 `SKILL.md.backup-*` 文件。依据 Codex 的项目级 skill 发现规则使用 `.agents/skills`，未创建不会被标准扫描的 `.codex/skills`，也无需在 `.codex/config.toml` 中重复登记路径。

2026-09-14 项目协作配置初始化：在工作区根目录新增 `AGENTS.md`、`.editorconfig`、`.gitignore` 和 `.codex/config.toml`。这些文件分别定义 Codex 项目约束、文本格式、版本控制忽略范围和项目级说明文件读取上限；未修改业务源码、WinForms Designer、资源、项目文件、运行配置或原生 DLL。未创建 `Directory.Build.props`，因为现有项目文件已经明确固定 .NET Framework 4.8、x86、警告级别和确定性构建，避免引入额外的隐式构建行为。

## 工作区根目录配置

以下文件位于包含解决方案目录的工作区根目录 `E:\WJ_DSRCProtocolTest_Desk_Rebuid\`。

| 路径 | 功能 | 是否手工维护 |
| --- | --- | --- |
| `AGENTS.md` | 项目地图与硬边界（当前 sln、窗体、服务、Native、受控 DLL/INI）。 | 是。 |
| `WJ_DSRCProtocolTest_Desk_Net/AGENTS.md` | 解决方案嵌套指令。 | 是。 |
| `WJ_DSRCProtocolTest_Desk_Net/artifacts/manual/` | 操作说明截图与 `build_manual.py`，不进入 C# 编译。 | 是；生成物 PNG 可覆盖，脚本需随界面改动维护。 |
| `.cursor/rules/project.mdc` | 每轮生效：范围、skill 路由、交付。 | 是。 |
| `.cursor/rules/csharp-dsrc-host.mdc` | 编辑 `WJ_DSRCProtocolTest_Desk_Net_20260920` 时：WinForms、测试大框、编码、DSRC。 | 是。 |
| `.cursor/skills/dsrc-csharp-host-developer/SKILL.md` | C# 上位机开发 skill（含确认计划后再改代码）。 | 是。 |
| `.cursor/skills/dsrc-csharp-host-migration/SKILL.md` | 旧 C++/MFC 用例迁移 skill。 | 是。 |
| `.cursor/skills/iteration-records/SKILL.md` | 「生成迭代记录」；仅显式调用。 | 是。 |
| `.cursor/skills/operation-manual-docx/SKILL.md` | 「生成操作说明」DOCX；仅显式调用。 | 是。 |
| `.editorconfig` | 工作区基础文本格式配置；C# 使用 4 空格，XML 类文件使用 2 空格，并明确不改变历史 INI 的编码和换行。 | 是；不得借配置批量重写历史文件。 |
| `.gitignore` | Git 忽略规则；排除 Visual Studio 状态、构建中间文件、验证输出、日志和临时测试产物，并显式保护项目原生 DLL 与正式 INI。 | 是；当前工作区尚未初始化为 Git 仓库。 |
| `.codex/config.toml` | 若存在：Codex 项目级配置。 | 是。 |

2026-09-14 工程目录迁移：完整工程已从 `E:\OBU上位机综合\WJ_DSRCProtocolTest_Desk_Net` 移动到 `E:\OBU上位机综合\WJ_DSRCProtocolTest_Desk_Rebuid\WJ_DSRCProtocolTest_Desk_Net`。解决方案、项目源码、运行依赖、配置、构建输出和 `.backups` 均随工程整体迁移；原目录已清理。本次未修改项目内部相对引用或业务代码。

2026-09-11 防碰撞 ICC0015 来源回归修正：最新实机日志显示普通车道交易已经成功，而防碰撞扣费仍返回 `93 02`。重新逐行核对原 `TEST_PROTOCOL.cpp::WJ_Trade_AntiCollision_EightSecond_new`，确认“读取余额、0015、0019”后仅检查完整回复末尾状态，并未调用 `DecodeTransferChannel_Rs` 或 `Decode_Apdu` 覆盖 `gVST.ICC0015`；随后 `ruanSM4CheckMAC1/ruanPsamCheckMAC1` 仍从 VST 预读的 `gVST.ICC0015` 取分散因子。`Services\LaneTransactionService.cs` 已恢复使用 `vst.Icc0015`，避免 `00 B0 95 01 2B` 返回内容造成一字节错位；`WanjiStandardTestForm.cs` 同步修正用例详情。修改前副本位于 `.backups\20260911-anti-collision-vst-icc0015\`。

2026-09-11 软算扣费终端交易序号修正：完整对照原 `ETC.cpp::Consume_GB` 和 `PSAM.cpp::ruanSM4CheckMAC1/ruanPsamCheckMAC1` 后，确认软算模式 `tradeType==1` 会在扣费前强制 `gTransSerial=00 00 06 C6`，MAC1 过程密钥分散因子也使用其末两字节 `06 C6`。`Services\LaneTransactionService.cs` 原扣费帧错误保留模板值 `00 02 79 55`，造成“计算 MAC1 的序号”与“OBU 实际验证的序号”不同，已统一改为 `00 00 06 C6`。`WanjiStandardTestForm.cs` 同步更新用例详情。修改前副本位于 `.backups\20260911-soft-consume-terminal-sequence\`。

2026-09-11 车道软算 MAC1 交易类型修正：运行日志显示备用车道交易在消费初始化成功后，扣费响应为 `93 02`。对照原 `ETC.cpp::Decode_Apdu` 确认软算入口固定使用 `IccTransType=0x09`；`SetMe.ini [TRADE_SET]/19` 第 5 字节是写入 0019 的通行状态，不能被解释成 MAC1 交易类型。`Services\SoftTradeCryptoService.cs` 已将 3DES/SM4 共用的 MAC1 交易类型固定为 `09`，交易金额和 0019 通行状态的组帧行为保持不变。修改前副本位于 `.backups\20260911-lane-mac1-transaction-type\`。

2026-09-11 防碰撞 ICC0015 分散来源历史记录（已被上方“防碰撞 ICC0015 来源回归修正”取代）：曾根据通用 `Decode_Apdu(ICC_0015)` 误将“读取余额、0015、0019”的第二条 APDU 返回值用于防碰撞 MAC1。进一步核对具体测试函数后确认该函数没有执行解码覆盖，因此当前实现已恢复使用 VST 预读 ICC0015。历史修改前副本位于 `.backups\20260911-anti-collision-read0015-source\`。

2026-09-11 SM4 消费根密钥修正：防碰撞扣费在金额标记修正后仍返回 `93 02`，逐行对照原 `PSAM.cpp::ruanSM4CheckMAC1` 确认其使用 `SecretKey_One[11]` 消费密钥 `B6 C4 30 72 D0 62 6F C5 02 C8 49 1F 1F 01 5B 87`。`Services\SoftTradeCryptoService.cs` 原先误用 `SecretKey_One[10]` 内部认证密钥 `AA 4E...`，已改为与原 OBU SM4 软算流程一致的 DPK3。修改前副本位于 `.backups\20260911-sm4-purchase-root-key\`。

2026-09-11 防碰撞 MAC1 金额标记修正：实机扣费返回 `93 02` 后，重新对照原 OBU `WJ_Trade_AntiCollision_EightSecond_new`，确认原流程在消费初始化前强制 `gINI.File_19[3]=0x06`，作为台发防碰撞五段计时标记。`Services\LaneTransactionService.cs` 现仅在防碰撞用例内将交易设置副本的金额低字节改为 `06`，并用同一份金额生成消费初始化帧和软算 MAC1；普通车道交易仍使用 `SetMe.ini` 金额。`WanjiStandardTestForm.cs` 同步更新用例详情。未修改 Designer 或 `.resx`；修改前副本位于 `.backups\20260911-anti-collision-amount-marker\`。

2026-09-11 OBU 防碰撞测试独立流程：`Services\LaneTransactionService.cs` 将防碰撞用例从通用静态帧序列改为原 OBU 模块的一轮动态软算交易：防碰撞 BST/VST、GetSecure、余额/0015/0019 读取、消费初始化并更新0019、软算 MAC1、交易认证、扣费、SetMMI、OtherFrames 读取五段计时及单向 EventReport。`WanjiStandardTestCaseHandlers.cs` 接入可停止的后台专用入口；`WanjiStandardTestForm.cs` 同步更新双击用例详情。配置 `[SET_139_AntiCol_num]/number` 按原逻辑表示计时项数，必须为 5；五项校正值均要求 `0 < t < 8 ms`，并保留 .NET 对每条 APDU 状态的安全校验。未修改 Designer 或 `.resx`；修改前副本位于 `.backups\20260911-anti-collision-original-flow\`。

2026-09-11 车道交易 VST 解析修正：原上位机按 BST 的系统信息长度 `0x1A` 解析 VST；VST 中的 `SysInfoFile=0x27` 是容器类型而不是 39 字节长度。`LaneTransactionService.ParseVst` 已按请求长度定位 43 字节 ICC0015，使用既有版本读取结果 Len=95 帧验证得到 ICC0015=43 字节；独立 x86 构建 0 警告、0 错误。

2026-09-10 车道交易改为原上位机软算：`Services\SoftTradeCryptoService.cs` 在进程内实现原 `ruanPsamCheckMAC1`/`ruanSM4CheckMAC1` 的 3DES/SM4 密钥分散、临时密钥、MAC1 和当前 UTC 交易时间生成；`WanjiStandardTestCaseHandlers.cs` 将界面 3DES/SM4 选择传入 `LaneTransactionService`，消费初始化帧算法标识和 GetSecure 算法标识同步切换。车道交易不再调用 `mwCardReader.dll`、`wdcrwv.dll` 或 `gmssl.dll`，这些 DLL 仅保留为用户提供的项目运行文件，供后续明确 ABI 的其他功能使用。SM4 已用标准向量 `0123456789ABCDEFFEDCBA9876543210` 验证输出 `681EDF34D206965E86B3E94F536E4246`。修改前副本位于 `.backups\20260910-soft-trade-algorithm\`；独立 x86 构建验证输出位于 `.verification-build-soft-algorithm\`。

2026-09-10 备用车道交易测试入口：`WanjiStandardTestCaseHandlers.cs` 保留 `备用测试项 > 1、车道交易测试` 的独立分发表；`WanjiStandardTestForm.cs` 负责把算法单选项传给软交易服务，服务层仍按原交易顺序执行 BST/VST、GetSecure、ESAM/ICC、消费初始化、MAC1、扣费、SetMMI 和单向 EventReport。硬件 PSAM/读卡器不参与该入口。

2026-09-09 测试信息清空和父级测试分组执行：`WanjiStandardTestForm.Designer.cs` 为“清空显示”按钮绑定 `buttonClearDisplay_Click`，`WanjiStandardTestForm.cs` 仅清空右侧测试信息 RichTextBox；单项“测试”按钮现在支持勾选可展开的父级测试项，按 TreeView 顺序递归收集全部叶子用例，并在当前异步用例完成后继续执行下一个。勾选单个叶子时仍只执行该叶子；多项勾选仍提示选择一个测试项或测试分组。修改前副本位于 `.backups\20260909-clear-display-parent-check\`。

2026-09-09 VST 随机避让配置次数和判定修正：`SetMe.ini` 的 `[SET_139_RandomDodge_ZeroSecond_num] / number` 已设置为 200；`WanjiStandardTestCaseHandlers.cs` 运行时读取该配置作为实际测试轮数，配置缺失或非法时默认 200。通过条件改为三个目标时间窗的计数均严格大于测试次数的 1/4，因此 200 次时每窗至少需要 51 次。修改前副本位于 `.backups\20260909-vst-config-count-quarter\`。

2026-09-09 VST 随机避让时间窗判定（次数和阈值已由本条后续修订）：`WanjiStandardTestCaseHandlers.cs` 按用户选择的原上位机兼容方案重构随机避让流程：固定执行原代码的 200 轮，每轮依次发送动态时间 BST、接收 VST、发送单向 EventReport，再调用独立 `OtherFrames` 请求/响应采样时间窗；EventReport 仍不调用接收函数。时间窗按原 OBU 公式和三段区间统计，三段计数均达到原表达式得到的 46 次才通过，同时校验 OtherFrames 请求返回码、响应返回码和前四字节长度。`WanjiStandardTestForm.cs` 同步更新双击逻辑说明；修改前副本位于 `.backups\20260909-vst-window-standard\`。

2026-09-09 VST 随机避让流程独立修正（已由本条后续时间窗标准替代）：`WanjiStandardTestCaseHandlers.cs` 将“2、VST随机避让测试”从通用随机 BST/VST 序列改为独立 `RunVstRandomDodgeTest`，按原 OBU `WJ_Trade_RandomDodge_ZeroSecond` 每轮生成带 Unix 时间的 30 字节 BST，接收 VST 后只发送单向 EventReport 释放链路；不再调用 `RunBstCompatibilityTest`，也不在 EventReport 后调用接收函数。协议日志对 EventReport 只记录 DLL 发送返回码，不输出响应占位内容。`WanjiStandardTestForm.cs` 同步更新双击流程说明。修改前副本位于 `.backups\20260909-vst-random-dodge-separate\`。

2026-09-09 测试树父级项双击流程：`WanjiStandardTestForm.cs` 调整 `treeViewTestItems_NodeMouseDoubleClick`，双击带“+”的父级测试项名称或展开图标都会弹出当前测试项流程说明，并收起默认展开状态；勾选框和“测试”按钮行为保持不变。修改前副本位于 `.backups\20260909-parent-doubleclick\`。

2026-09-08 OBU 标准测试首批流程接入（随机避让次数描述已由最新条目修订）：`WanjiStandardTestCaseHandlers.cs` 将 VST 随机避让、防碰撞、三类预读信息和 255S 保持测试从占位入口接入独立协议函数；历史版本随机避让曾读取 `[SET_139_RandomDodge_ZeroSecond_num]`，预读流程按原 BST/TransferChannel 顺序执行，EventReport 仅发送不等待响应，255S 从 `BST_Locked.ini` 读取 BST 与 00/01 期望判定。防碰撞和预读的完整交易字段仍按原 DLL 能力逐步细化，当前已接入可执行的 BST/VST 与核心透传步骤。修改前源码和本说明备份于 `.backups\20260908-obu-standard-first-cases\`。

2026-09-08 OBU 标准测试扩展流程：在上述首批流程基础上，继续接入 OBU 信道选择（0/1 信道）、GetSecure 四种许可/密钥标识组合、TransferChannel 1～7 条 IC APDU 与 1～3 条 ESAM APDU、混合 APDU 用例、SetMMI 参数 00～04、拼帧指令和 255S 保持测试；每个测试用例保留独立 `RunTestCaseXXX` 入口并通过可取消的后台协议序列执行，`WanjiStandardTestForm.cs` 同步提供这些用例的双击逻辑说明。EventReport 统一走 0005 单向发送，不调用接收函数；255S 从 exe 同级 `BST_Locked.ini` 读取帧和 00/01 判定。当前已接入核心链路与配置驱动判定，尚未覆盖 11 项之后的交易广播、不同 MAC、指令集等用例。修改前说明副本位于 `.backups\20260908-obu-standard-extended\`。

说明：文档中更早的日期条目保留历史变更轨迹；若与当前实现描述冲突，以本条、`OBU BST 兼容性来源纠正` 条目和当前源码为准。

2026-09-08 OBU BST 兼容性来源纠正：此前误参考 CPC 模块 `TEST_CPC_TRADE.cpp::WJ_BST_Compatibility_Test`。重新沿原 OBU 界面 `TRADE_DIALOG.cpp` 的按钮分发定位到 `TEST_PROTOCOL.cpp::WJ_ProtocolTest_Bst` 后，`WanjiStandardTestCaseHandlers.cs` 改为读取 `[SET_Bst] / number`，每次随机生成一条 OBU BST 并接收 VST，进度显示“第 x/配置次数次”，每次间隔 20 ms，整个用例不发送 EventReport；`WanjiStandardTestForm.cs` 同步更新双击逻辑说明。修改前文件备份于 `.backups\20260908-bst-progress-no-eventreport\`。

2026-09-08 EventReport 单向日志修正：确认 `Services\DesktopCommService.cs` 的 `0005 EventReport` 路由只调用 DLL 发送函数、未调用接收函数；修正 `Services\TestResultFileWriter.cs`，单向帧只输出“下行发送”和 DLL 发送返回码，不再生成零长度响应或“响应未调用”占位内容；`WanjiStandardTestForm.cs` 的 BST 逻辑说明同步明确 OBU 无需回复 EventReport。对应协议规则已加入 `dsrc-csharp-host-developer` skill。修改前文件备份于 `.backups\20260908-eventreport-one-way\`。

2026-09-08 测试大框统一停止：`WanjiStandardTestForm.cs` 将原 BST 专用取消状态提升为当前测试大框统一取消入口，“测试”和“全部测试”运行期间统一管理按钮状态；OBU 版本号读取改为后台执行并接入同一停止按钮。`WanjiStandardTestCaseHandlers.cs` 的 BST 用例复用统一令牌；`Services\DesktopCommService.cs` 在版本读取的 BST/VST、两步 71 认证和最终版本读取 DLL 调用前检查停止状态。其他尚未实现的占位用例后续实现时必须复用该入口。修改前文件备份于 `.backups\20260908-unified-test-stop\`。

2026-09-08 BST 千次测试、停止和间隔修正：`SetMe.ini` 的 OBU `[SET_Bst] / number` 为 1000；`WanjiStandardTestForm.Designer.cs` 显式绑定“停止测试”按钮；`WanjiStandardTestForm.cs` 发出当前 BST 后台流程的取消信号；`WanjiStandardTestCaseHandlers.cs` 在每帧和每轮入口检查停止状态，并按原 OBU `WJ_ProtocolTest_Bst` 的 20 ms 间隔替换阻塞休眠。原 CPC 配置节不再用于 OBU BST。修改前文件备份于 `.backups\20260908-bst-stop-interval\`。

2026-09-08 原上位机配置接入：从原项目源码目录按原字节复制 `SetMe.ini`、`BST_Locked.ini` 和 `BST_Locked_Sutong.ini`，项目构建时将三者复制到 exe 同级目录；OBU BST 读取 `[SET_Bst] / number`，随机避让、防碰撞和预读流程读取各自 `[SET_139_*_num]` 配置，255S 保持测试读取 `BST_Locked.ini`。修改前源码、项目文件和本文档备份于 `.backups\20260908-legacy-config\`。

本文档说明当前上位机目录内各文件和目录的职责。后续每次新增、修改、移动或删除项目文件时，必须同步更新本文档。

2026-09-08 万集标准测试 UI：`MainForm.Designer.cs` 在“万集标准测试”页增加从左到右的“万集 OBU 标准测试、北京地标协议测试、万集 CPC 卡标准测试、守望者播报测试”四个大型入口按钮并显式绑定事件；`MainForm.cs` 负责打开类别窗口；新增 `WanjiStandardTestForm.cs`、`WanjiStandardTestForm.Designer.cs` 和 `WanjiStandardTestForm.resx`，提供大型测试项内容容器，具体测试项尚未实现；项目文件已纳入新窗体。修改前文件备份于项目 `.backups\20260908-wanji-standard-ui\`。

2026-09-08 万集 OBU 标准测试 UI：`WanjiStandardTestForm.Designer.cs` 将万集 OBU 标准测试窗口内容调整为 42% 测试项选择区、12% 操作选项区和 46% 测试信息显示区；操作区加入“交易_校验ESAM”、3SDE/SM4 算法选择、“测试”“全部测试”“停止测试”“清空显示”控件，当前均只完成界面搭建，未接入测试行为。修改前文件备份于项目 `.backups\20260908-wanji-obu-content-layout\`。

2026-09-08 万集 OBU 标准测试用例和版本读取：`WanjiStandardTestForm.Designer.cs` 按原上位机迁移“通用测试项、单片式专用测试项、双片式专用测试项、地区专用测试项、备用测试项”及其测试用例名称，默认选中“0、OBU版本号读取”；`WanjiStandardTestForm.cs` 为“测试”按钮增加版本号读取入口，其余用例仅显示“逻辑将在后续添加”的预留提示；`Services\DesktopCommService.cs` 实现 BST/VST、版本读取认证（71 指令）和 TransferChannel 读取流程，实际设备通信仍复用独立项目内的 `DeskTopComm.dll`。修改前源码和本说明备份于项目 `.backups\20260908-wanji-obu-version-read\`。

2026-09-08 测试逻辑弹窗和结果文件：`WanjiStandardTestForm.Designer.cs` 为测试用例名称绑定仅作用于标签区域的双击事件；`TestCaseLogicForm.cs`、`TestCaseLogicForm.Designer.cs` 提供测试逻辑说明弹窗，当前完整展示 OBU 版本号读取的 5.8G 帧流程，其余用例仅保留扩展接口说明；`Services\DesktopCommService.cs` 为版本读取结果收集 BST/VST、71 认证和版本读取的完整请求/响应帧；`Services\TestResultFileWriter.cs` 在 exe 同级 `TestResults\` 下按“时间_用例名_测试结果.txt”生成 UTF-8 结果文件。修改前源码和项目文件备份于 `.backups\20260908-obu-case-logic-result-log\`。

2026-09-08 测试结果存储开关和截图：`WanjiStandardTestForm.Designer.cs` 在操作选项区最上方增加默认不勾选的“存储测试结果”复选框；`WanjiStandardTestForm.cs` 仅在勾选后保存结果 TXT，并在 OBU 版本号读取完成后调用截图服务；`Services\TestResultScreenshotWriter.cs` 将测试大框完整界面保存到 exe 同级 `TestScreenshots\`，文件名为“用例名_测试时间_测试结果.png”。其他用例暂不生成截图。修改前文件备份于 `.backups\20260908-store-test-result-screenshot\`。

2026-09-08 全用例结果截图：`WanjiStandardTestForm.cs` 将测试大框截图扩展到所有被执行的测试用例；勾选“存储测试结果”后，已实现的 OBU 版本号读取和其余暂未实现用例均生成 TXT 和 PNG，未勾选时不生成任何结果目录或文件。

2026-09-08 WinForms 设计器兼容修复：`WanjiStandardTestForm.Designer.cs` 将测试树节点改为标准 `new TreeNode(...)` 和根节点 `AddRange` 结构，避免设计器无法解析由 `Nodes.Add()` 返回值赋给局部变量而导致的“变量未声明或未赋值”错误；测试用例名称、层级和双击事件保持不变。修改前 Designer 备份于 `.backups\20260908-designer-tree-compatibility\`。

2026-09-08 主窗体设计器类型解析兼容：`MainForm.Designer.cs` 使用 `global::WJ_DSRCProtocolTest_Desk_Net.Controls.TransparentTestControl` 显式声明和实例化透传控件，避免设计器在解析嵌套命名空间时报告找不到类型或 `transparentTestControl` 未声明；`TransparentTestControl` 的公共无参构造和独立命名空间保持不变。修改前文件备份于 `.backups\20260908-mainform-designer-compatibility\`。

2026-09-08 四个标准测试独立大框：`MainForm.Designer.cs` 将四个入口按钮分别绑定到独立事件；`MainForm.cs` 分别打开 OBU、北京地标、万集 CPC 卡和守望者播报四种测试窗体。新增 `BeijingLocalStandardTestForm`、`WanjiCpcStandardTestForm`、`WatchmanBroadcastTestForm` 及其 Designer 文件，后三个窗体当前提供独立测试项树和测试信息占位区，后续可分别添加测试内容；OBU 继续使用已接入版本号读取逻辑的 `WanjiStandardTestForm`。修改前文件备份于 `.backups\20260908-independent-standard-forms\`。

2026-09-10 OBU 广播 BST 响应测试第一批：`WanjiStandardTestCaseHandlers.cs` 将“获取车辆信息后发送BST”和“消费初始化后发送BST”从占位入口改为独立协议流程，按原 OBU 模块依次执行首次 BST/VST、GetSecure、可选消费初始化，并以第二次广播 BST 发送成功且无 VST 响应作为通过条件；消费初始化响应必须以 `90 00 00` 结束。通用协议执行器的预期无响应日志改为适用于广播 BST 等负向用例，同时保持 EventReport 单向发送逻辑不变。`WanjiStandardTestForm.cs` 同步增加两个用例的双击流程和判断标准说明。

2026-09-10 OBU 不同 MAC 响应测试第二批：`WanjiStandardTestCaseHandlers.cs` 将三个“交易过程中响应不同MAC”入口改为独立协议流程，依次执行 BST/VST、GetSecure 和错误 MAC 的读取 0015 TransferChannel，并按原 OBU 模块以“请求发送成功且 OBU 无响应”为通过条件；三种错误地址分别为当前 MAC 末字节加一、全 `FF`、当前 MAC 第三字节加一。`Services/DesktopCommService.cs` 为统一透传入口增加显式会话 MAC 变体参数，确保负向帧基于本次 VST 返回 MAC 生成且不会被普通动态 MAC 更新覆盖。`WanjiStandardTestForm.cs` 同步补充三个用例的双击流程、错误地址规则和判断标准。

2026-09-10 指令集测试第三批：`WanjiStandardTestCaseHandlers.cs` 将指令集第 1～17 项从占位入口改为独立协议流程，按原上位机执行 BST/VST、选择 ESAM 3F00、目标 APDU、SetMMI 和 EventReport；正常项检查 `90 00 00`，边界/错误参数项检查 `67 00 00`、`6A 86 00`、`6E 00 00`、`6D 00 00`。`WanjiStandardTestForm.cs` 增加该组用例的双击流程与状态码判定说明。

2026-09-08 测试用例函数分发：`WanjiStandardTestForm.cs` 的 `buttonSingleTest_Click` 仅负责校验叶子节点并按完整树路径分发；`WanjiStandardTestCaseHandlers.cs` 为当前 94 个叶子测试用例建立独立函数入口，OBU 版本号读取调用真实逻辑，其余函数调用统一占位接口，后续可逐个填入对应 5.8G 测试流程。修改前文件备份于 `.backups\20260908-testcase-routing\`。
2026-09-08 BST 兼容性测试：`WanjiStandardTestCaseHandlers.cs` 将“1、BST兼容性测试”映射到独立的 `RunBstCompatibilityTest`，按原上位机实际 23 条 BST 负载顺序执行 BST/VST、从 VST 前四字节取得 MACID 后发送 EventReport，并保留 10200 ms/200 ms 分组等待；每条请求、响应和最终结果均进入统一测试结果文件/截图流程。双击用例名称显示该流程说明。修改前源码和本说明备份于 `.backups\20260908-bst-compatibility\`。
2026-09-08 单项测试勾选路由：`WanjiStandardTestForm.cs` 的单测按钮优先读取唯一勾选的叶子测试项，再分发到对应独立函数；仅在没有勾选项时回退到当前选中的叶子节点，多选时提示用户，避免勾选 BST 后误执行默认的 OBU 版本号读取。修改前源码和本说明备份于 `.backups\20260908-checkbox-routing\`。
2026-09-08 BST 后台执行：`WanjiStandardTestCaseHandlers.cs` 将 OBU BST 兼容性测试的 DLL 通信和原流程等待移到后台任务，进度通过 UI 线程安全回写，最终结果文件和截图仍在 UI 线程完成，避免通信等待阻塞测试窗体。修改前源码和本说明备份于 `.backups\20260908-bst-background-execution\`。

2026-09-08 主窗体布局：`MainForm.Designer.cs` 将左侧台发设置通道的固定宽度由 310 px 缩短为 280 px，为右侧功能区增加 30 px 显示空间。修改前 Designer 和本说明备份于项目 `.backups\20260908-narrow-station-panel\`。

2026-09-07 字段显示格式：`Controls\TransparentTestControl.cs` 使用“中文描述：内容”显示解析结果，去除字节位置和原始数据行地址；协议自身的文件读取偏移字段仍保留。修改前源码和本说明备份在项目 `.backups\chinese-fields-20260907\` 的同名文件中。

2026-09-07 实时帧修正：`Controls\TransparentTestControl.cs` 增加公共链路头、BST/VST 固定字段、GetSecure、TransferChannel APDU、SetMMI 与 EventReport 字段解析，短帧提示截断，未支持的可选区保留原始数据。`Services\DesktopCommService.cs` 在启用 MAC 更新时使用成功 VST 的地址更新后续请求，并返回请求缓冲区快照用于日志；该快照是 DLL 调用数据，不是独立空口抓包。上述两个源码及本说明的修改前副本位于项目 `.backups\frame-details-20260907\` 下的同名文件。

2026-09-07：在 `Controls\TransparentTestControl.Designer.cs` 中恢复缺失的人工帧列表 `listBoxFrameSequence`，跨当前布局的前三列、前四行，并绑定选中帧加载到 BUF 的事件。修复前的 Designer 和本说明分别备份于项目目录 `.backups\restore-frame-list-20260907-141457\TransparentTestControl.Designer.cs` 和 `.backups\restore-frame-list-20260907-141457\项目文件说明.md`。

## 解决方案根目录

| 路径 | 功能 | 是否手工维护 |
| --- | --- | --- |
| `WJ_DSRCProtocolTest_Desk_Net.sln` | Visual Studio 解决方案入口，登记并组织上位机项目及 Debug/Release、x86 构建配置。 | 是；通过 Visual Studio 或谨慎编辑维护。 |
| `项目文件说明.md` | 当前文件。记录解决方案内源码、配置、依赖、备份和生成文件的用途。 | 是；每次项目文件变动后同步更新。 |

## 项目源码和配置

以下文件位于 `WJ_DSRCProtocolTest_Desk_Net\` 项目目录。

| 路径 | 功能 | 是否手工维护 |
| --- | --- | --- |
| `WJ_DSRCProtocolTest_Desk_Net.csproj` | .NET Framework 4.8 WinForms 项目配置；目标平台为 x86，登记 C# 源码、窗体资源、系统引用，并将 `Native\DeskTopComm.dll`、`mwCardReader.dll`、`wdcrwv.dll`、`gmssl.dll` 及原上位机 INI 配置复制到输出目录。 | 是；修改前检查编码并保留可恢复差异。 |
| `SetMe.ini` | 从原上位机按原字节复制的运行参数配置；当前 OBU BST 兼容性测试读取 `[SET_Bst]` 下的 `number` 作为测试次数，VST 随机避让读取 `[SET_139_RandomDodge_ZeroSecond_num]` 下的 `number` 作为实际轮数（当前为 200），读取 `warn` 作为原配置保留项；CPC 专用节不参与 OBU BST 测试。 | 可配置；保持原 ANSI/GBK 编码和节名、键名。 |
| `BST_Locked.ini` | 从原上位机按原字节复制的 BST 锁定配置；255S 保持测试读取其中的 BST 帧和 00/01 期望判定。 | 可配置；保持原编码和字段结构。 |
| `BST_Locked_Sutong.ini` | 从原上位机按原字节复制的苏通 BST 锁定配置，供后续对应测试逻辑按原行为接入。 | 可配置；保持原编码和字段结构。 |
| `Program.cs` | 程序入口；初始化 WinForms 环境并启动 `MainForm`。 | 是。 |
| `MainForm.cs` | 主窗体行为代码；处理窗体加载/关闭、串口实时枚举、端口开关、台发初始化、DLL 日志开关、输入校验和状态显示，并把主窗体持有的台发服务交给透传测试控件复用；处理万集标准测试四个入口按钮并打开对应类别窗口。 | 是；只放行为及调用逻辑。 |
| `MainForm.Designer.cs` | 主窗体设计器代码；声明并配置左侧台发设置区、串口选择和监测状态、初始化参数、功能页签、状态栏、控件层级、布局和事件绑定；透传页内嵌独立的 `TransparentTestControl`；万集标准测试页横向排列四个大型类别入口按钮。 | 是，但应由 WinForms 设计器管理；结构性修改前必须备份。 |
| `MainForm.resx` | 主窗体资源文件，供 WinForms 设计器保存窗体资源和本地化数据；当前不承载业务逻辑。 | 非必要不手工修改。 |
| `BeijingLocalStandardTestForm.cs` / `.Designer.cs` | 北京地标协议测试独立大框；按原 `ONECHIP_DIALOG` 活动目录显示四类测试树，提供父子勾选、叶子顺序调度、停止、进度和信息显示，并通过完整路径注册后续异步协议处理器。 | 是；行为与 Designer 分离，真实协议处理器尚未登记。 |
| `WanjiCpcStandardTestForm.cs` / `.Designer.cs` | 万集 CPC 卡标准测试独立大框及其静态布局；当前为独立测试项树和测试信息占位界面，具体逻辑待后续添加。 | 是；行为与 Designer 分离。 |
| `WatchmanBroadcastTestForm.cs` / `.Designer.cs` | 守望者播报测试独立大框及其静态布局；当前为独立测试项树和测试信息占位界面，具体逻辑待后续添加。 | 是；行为与 Designer 分离。 |
| `WanjiStandardTestForm.cs` | 万集标准测试类别窗口的行为代码；处理“测试”按钮、父级测试分组按树顺序执行、清空测试信息显示、按选项保存所有用例结果 TXT 和测试大框 PNG，以及在测试项名称或展开图标区域双击叶子/带“+”的父级节点时显示流程说明弹窗。 | 是；只放行为及调用逻辑。 |
| `WanjiStandardTestForm.Designer.cs` | 万集标准测试类别窗口的静态设计器布局；显式迁移原上位机测试用例树，绑定测试按钮、清空显示按钮和测试用例名称双击事件，并在操作区顶部放置默认关闭的“存储测试结果”复选框。 | 是，但应由 WinForms 设计器管理；结构性修改前必须备份。 |
| `WanjiStandardTestCaseHandlers.cs` | 万集 OBU 标准测试全部叶子用例的函数分发表和独立实现入口；OBU 版本号读取、OBU BST、独立 VST 随机避让、防碰撞、预读信息、信道选择、GetSecure、TransferChannel、SetMMI、拼帧和 255S 保持测试已接入独立协议流程；备用车道交易测试入口读取界面算法选择并调用进程内软算交易服务。测试信息区只显示进度、步骤、判定和结果，完整帧由结果文档保存。 | 是；协议步骤和软算入口按原上位机流程维护。 |
| `TestCaseLogicForm.cs` | 测试逻辑说明弹窗行为；显示当前用例名称和逻辑文本，不启动设备通信。 | 是；只放行为及调用逻辑。 |
| `TestCaseLogicForm.Designer.cs` | 测试逻辑说明弹窗的静态布局；包含用例标题、只读逻辑文本框和关闭按钮。 | 是，但应由 WinForms 设计器管理；结构性修改前必须备份。 |
| `WanjiStandardTestForm.resx` | 万集标准测试类别窗口的 WinForms 设计器资源文件；当前仅保留标准资源头。 | 非必要不手工修改。 |
| `Properties\AssemblyInfo.cs` | 程序集元数据，包括产品名、公司、COM 可见性、GUID 和程序集版本。 | 是。 |
| `Controls\TransparentTestControl.cs` | 透传测试页行为代码；下方维护人工添加且可修改/排序的待发送帧，上方记录 DLL 实时产生的上下行帧，点击后在左侧解析；打开、保存和清空仅操作实时帧，测试使用内部固定 500 ms 超时。 | 是；不自行打开设备，复用主窗体台发服务。 |
| `Controls\TransparentTestControl.Designer.cs` | 透传测试 UserControl 的静态布局和事件绑定；实时帧与人工帧列表均为左侧长列表加右侧竖排按钮，BUF 区右侧纵向放置“修改”和“清空 BUF”，“测试”位于交易帧右侧；不再显示 `+BST` 至 `+ICC` 快捷组帧控件及超时选择。 | 是，但应由 WinForms 设计器管理；结构性修改前必须备份。 |

## 设备 DLL 接入层

| 路径 | 功能 | 是否手工维护 |
| --- | --- | --- |
| `Native\DesktopCommNativeMethods.cs` | `DeskTopComm.dll` 的集中 P/Invoke 声明，封装打开/关闭、初始化、日志以及 BST/VST、GetSecure、TransferChannel、SetMMI、EventReport、OtherFrames、ICC/CPC 透传函数。 | 是；必须与 DLL 的 x86 ABI、stdcall 调用约定和参数类型保持一致。 |
| `Native\LegacyLaneNativeMethods.cs` | 三个用户 DLL 的集中 P/Invoke 声明；包含 `wdcrwv.dll` 的 3DES/MAC、`mwCardReader.dll` 的读卡器入口、`gmssl.dll` 版本入口和无副作用的加载验证。 | 是；必须以已核验的导出名、x86 和调用约定为准。 |
| `Native\DeskTopComm.dll` | 台发通信的原生 x86 动态库，是新项目内部持有的运行依赖副本；构建时复制到 EXE 同目录。 | 否；由用户提供或明确替换，不修改二进制内容。 |
| `Native\mwCardReader.dll` | 用户提供的 x86 读卡器功能 DLL；当前由车道交易适配层做加载验证，并预留读卡器 APDU ABI，构建时复制到 EXE 同目录。 | 否；只复制和调用导出接口，不修改二进制内容。 |
| `Native\wdcrwv.dll` | 用户提供的 x86 读卡器驱动/密码算法 DLL；当前软车道交易不加载、不调用，保留供后续用户明确要求且 ABI 已核验的功能使用。 | 否；只复制，不修改二进制内容。 |
| `Native\gmssl.dll` | 用户提供的 x86 加密支持 DLL；当前软车道交易不加载、不调用，C# 已在 `SoftTradeCryptoService.cs` 内实现本次所需 SM4。 | 否；只复制，不修改二进制内容。 |
| `Services\DesktopCommService.cs` | 台发业务服务层；管理 DLL 句柄及初始化状态、转换初始化参数和 Unix 时间、解释返回码，并按本地类型 `0001`～`0007` 路由透传请求/响应；同时提供 OBU 版本号读取所需的 BST/VST、71 指令认证、TransferChannel 请求及版本文本结果模型。 | 是。 |
| `Services\LegacyLaneDllService.cs` | 历史原生依赖适配层；保留用户 DLL 的 ABI 声明和显式调用封装，但当前软车道交易不引用其算法入口。 | 是；只有用户明确要求且 ABI 核验后才能复用。 |
| `Services\SoftTradeCryptoService.cs` | 车道交易软算法服务；实现原上位机 3DES/SM4 软交易的密钥分散、MAC1 和交易时间计算，不打开读卡器、不调用 PSAM。 | 是；密钥、分散因子和算法步骤必须与原上位机确认结果一致。 |
| `Services\LaneTransactionService.cs` | 备用车道/门架及播报金额交易业务层；普通交易读取 `SetMe.ini`，播报金额使用原用例九档固定金额，按各自原流程组帧/收帧，解析 VST ICC0015，调用选定的 3DES/SM4 软算法计算 MAC1，执行 Consume、SetMMI（原流程包含时）和单向 EventReport，并返回完整交互帧结果。 | 是；真实设备流程与语音仍需连接 RSU/OBU 实机确认。 |
| `Services\TestResultFileWriter.cs` | 测试结果文件服务；将版本号读取的最终结果、版本号、DLL 返回码和完整请求/响应帧写入 exe 同级 `TestResults\` 目录。 | 是。 |
| `Services\TestResultScreenshotWriter.cs` | 测试截图服务；使用 WinForms `DrawToBitmap` 截取测试大框完整界面，并按用例名、完成时间和成功/失败状态保存 PNG，供所有被执行用例调用。 | 是。 |

## 可恢复备份

`.backups\` 保存设计器或关键源码结构性修改前的可恢复副本，不参与项目编译，也不能覆盖用户较新的源码。

| 路径 | 功能 |
| --- | --- |
| `.backups\20260910-backup-lane-trade-attempt\WanjiStandardTestCaseHandlers.cs` | 接入备用车道交易测试独立入口和安全阻断说明前的用例处理器备份。 |
| `.backups\20260910-backup-lane-trade-attempt\WanjiStandardTestForm.cs` | 增加原车道交易流程与当前 DLL 能力缺口说明前的测试大框行为备份。 |
| `.backups\20260910-backup-lane-trade-attempt\项目文件说明.md` | 本次备用车道交易测试试接入前的项目文件说明备份。 |
| `.backups\20260908-wanji-obu-content-layout\WanjiStandardTestForm.cs` | 调整万集 OBU 标准测试内容区布局前的窗口行为备份。 |
| `.backups\20260908-wanji-obu-content-layout\WanjiStandardTestForm.Designer.cs` | 增加三列测试 UI 控件前的窗口设计器备份。 |
| `.backups\20260908-wanji-obu-content-layout\项目文件说明.md` | 本次万集 OBU 标准测试内容布局调整前的项目文件说明备份。 |
| `.backups\20260908-wanji-standard-ui\MainForm.cs` | 增加万集标准测试类别入口行为前的主窗体行为备份。 |
| `.backups\20260908-wanji-standard-ui\MainForm.Designer.cs` | 将万集标准测试页改为四个大型入口按钮前的主窗体设计器备份。 |
| `.backups\20260908-wanji-standard-ui\WJ_DSRCProtocolTest_Desk_Net.csproj` | 登记万集标准测试类别窗口前的项目文件备份。 |
| `.backups\20260908-wanji-standard-ui\项目文件说明.md` | 本次万集标准测试 UI 搭建前的项目文件说明备份。 |
| `.backups\20260908-wanji-obu-version-read\WanjiStandardTestForm.cs` | 迁移 OBU 测试用例名称并接入版本号读取行为前的窗口行为备份。 |
| `.backups\20260908-wanji-obu-version-read\WanjiStandardTestForm.Designer.cs` | 迁移 OBU 测试用例树和测试按钮事件前的窗口设计器备份。 |
| `.backups\20260908-wanji-obu-version-read\MainForm.cs` | 将共享台发服务传入万集 OBU 标准测试窗口前的主窗体行为备份。 |
| `.backups\20260908-wanji-obu-version-read\DesktopCommService.cs` | 增加 OBU 版本号读取和认证流程前的台发服务备份。 |
| `.backups\20260908-wanji-obu-version-read\项目文件说明.md` | 本次 OBU 测试用例和版本读取改动前的项目文件说明备份。 |
| `.backups\20260908-obu-case-logic-result-log\WanjiStandardTestForm.cs` | 增加用例名称双击逻辑说明和测试结果文件写入前的窗口行为备份。 |
| `.backups\20260908-obu-case-logic-result-log\WanjiStandardTestForm.Designer.cs` | 绑定用例名称双击事件前的窗口设计器备份。 |
| `.backups\20260908-obu-case-logic-result-log\DesktopCommService.cs` | 增加版本读取完整交互帧收集前的台发服务备份。 |
| `.backups\20260908-obu-case-logic-result-log\WJ_DSRCProtocolTest_Desk_Net.csproj` | 登记测试逻辑弹窗和结果文件服务前的项目文件备份。 |
| `.backups\20260908-obu-case-logic-result-log\项目文件说明.md` | 本次测试逻辑弹窗和结果文件功能修改前的项目文件说明备份。 |
| `.backups\20260908-store-test-result-screenshot\WanjiStandardTestForm.cs` | 增加存储结果开关和 OBU 测试截图入口前的窗口行为备份。 |
| `.backups\20260908-store-test-result-screenshot\WanjiStandardTestForm.Designer.cs` | 在操作区增加“存储测试结果”复选框前的窗口设计器备份。 |
| `.backups\20260908-store-test-result-screenshot\TestResultFileWriter.cs` | 增加可选存储和截图联动前的结果文件服务备份。 |
| `.backups\20260908-store-test-result-screenshot\WJ_DSRCProtocolTest_Desk_Net.csproj` | 登记截图服务前的项目文件备份。 |
| `.backups\20260908-store-test-result-screenshot\项目文件说明.md` | 本次存储开关和测试截图功能修改前的项目文件说明备份。 |
| `.backups\20260908-all-test-result-screenshot\WanjiStandardTestForm.cs` | 将截图范围扩展到全部测试用例前的窗口行为备份。 |
| `.backups\20260908-all-test-result-screenshot\项目文件说明.md` | 本次全用例结果截图修改前的项目文件说明备份。 |
| `.backups\20260909-parent-doubleclick\WanjiStandardTestForm.cs` | 修改测试树父级节点双击行为前的窗口行为备份。 |
| `.backups\20260909-parent-doubleclick\项目文件说明.md` | 修改测试树父级节点双击行为前的项目文件说明备份。 |
| `.backups\20260909-vst-random-dodge-separate\WanjiStandardTestCaseHandlers.cs` | 将 VST 随机避让流程从通用 BST/VST 序列拆分前的用例处理备份。 |
| `.backups\20260909-vst-random-dodge-separate\WanjiStandardTestForm.cs` | 更新 VST 随机避让双击流程说明前的窗口行为备份。 |
| `.backups\20260909-vst-random-dodge-separate\项目文件说明.md` | 本次 VST 随机避让流程独立修正前的项目文件说明备份。 |
| `.backups\20260909-vst-window-standard\WanjiStandardTestCaseHandlers.cs` | 按原时间窗统计标准改造 VST 随机避让流程前的用例处理备份。 |
| `.backups\20260909-vst-window-standard\WanjiStandardTestForm.cs` | 更新 VST 随机避让时间窗判定说明前的窗口行为备份。 |
| `.backups\20260909-vst-window-standard\项目文件说明.md` | 本次 VST 随机避让时间窗标准改造前的项目文件说明备份。 |
| `.backups\20260909-vst-config-count-quarter\WanjiStandardTestCaseHandlers.cs` | 改为读取随机避让配置次数并采用“每窗严格大于总次数四分之一”判定前的用例处理备份。 |
| `.backups\20260909-vst-config-count-quarter\WanjiStandardTestForm.cs` | 更新随机避让配置次数和通过标准说明前的窗口行为备份。 |
| `.backups\20260909-vst-config-count-quarter\SetMe.ini` | 将随机避让测试次数从 15 改为 200 前的配置备份。 |
| `.backups\20260909-vst-config-count-quarter\项目文件说明.md` | 本次随机避让配置次数和判定标准修改前的项目文件说明备份。 |
| `.backups\20260909-clear-display-parent-check\WanjiStandardTestForm.cs` | 增加测试信息清空和父级测试分组顺序执行前的窗口行为备份。 |
| `.backups\20260909-clear-display-parent-check\WanjiStandardTestForm.Designer.cs` | 绑定“清空显示”按钮事件前的窗口 Designer 备份。 |
| `.backups\20260909-clear-display-parent-check\项目文件说明.md` | 本次清空显示和父级测试分组执行修改前的项目文件说明备份。 |
| `.backups\20260908-obu-standard-extended\项目文件说明.md` | 接入 OBU 标准测试扩展流程后，本次项目说明的可恢复副本。 |
| `.backups\20260908-designer-tree-compatibility\WanjiStandardTestForm.Designer.cs` | 修复测试树节点写法以兼容 WinForms 设计器前的窗口设计器备份。 |
| `.backups\20260908-designer-tree-compatibility\项目文件说明.md` | 本次 WinForms 设计器兼容修复前的项目文件说明备份。 |
| `.backups\20260908-mainform-designer-compatibility\MainForm.Designer.cs` | 使用全局限定类型修复主窗体透传控件设计器解析前的主窗体 Designer 备份。 |
| `.backups\20260908-mainform-designer-compatibility\TransparentTestControl.cs` | 主窗体设计器类型兼容检查时保留的透传控件行为备份。 |
| `.backups\20260908-independent-standard-forms\MainForm.cs` | 将四个标准测试入口改为分别打开独立窗体前的主窗体行为备份。 |
| `.backups\20260908-independent-standard-forms\MainForm.Designer.cs` | 将四个入口按钮分别绑定独立事件前的主窗体 Designer 备份。 |
| `.backups\20260908-independent-standard-forms\WJ_DSRCProtocolTest_Desk_Net.csproj` | 登记三个独立标准测试窗体前的项目文件备份。 |
| `.backups\20260908-testcase-routing\WanjiStandardTestForm.cs` | 将单测按钮改为按完整测试树路径分发到独立用例函数前的窗口行为备份。 |
| `.backups\20260908-testcase-routing\WJ_DSRCProtocolTest_Desk_Net.csproj` | 登记测试用例函数分发表文件前的项目文件备份。 |
| `.backups\20260908-bst-compatibility\WanjiStandardTestCaseHandlers.cs` | 将 BST 兼容性测试从占位入口替换为实际 23 条帧序列前的用例函数备份。 |
| `.backups\20260908-bst-compatibility\WanjiStandardTestForm.cs` | 增加 BST 兼容性双击逻辑说明前的测试窗体行为备份。 |
| `.backups\20260908-bst-compatibility\项目文件说明.md` | 本次 BST 兼容性测试功能修改前的项目文件说明备份。 |
| `.backups\20260908-checkbox-routing\WanjiStandardTestForm.cs` | 修正单测按钮优先使用勾选叶子测试项前的测试窗体行为备份。 |
| `.backups\20260908-checkbox-routing\项目文件说明.md` | 本次单项测试勾选路由修改前的项目文件说明备份。 |
| `.backups\20260908-bst-background-execution\WanjiStandardTestCaseHandlers.cs` | 将 BST 通信和等待移入后台任务前的用例函数备份。 |
| `.backups\20260908-bst-background-execution\项目文件说明.md` | 本次 BST 后台执行修改前的项目文件说明备份。 |
| `.backups\20260908-legacy-config\WanjiStandardTestCaseHandlers.cs` | 接入原上位机配置和 BST 测试次数读取前的用例函数备份。 |
| `.backups\20260908-legacy-config\WJ_DSRCProtocolTest_Desk_Net.csproj` | 登记三个原上位机 INI 配置文件前的项目文件备份。 |
| `.backups\20260908-legacy-config\项目文件说明.md` | 本次原上位机配置接入前的项目文件说明备份。 |
| `.backups\20260908-bst-stop-interval\WanjiStandardTestForm.cs` | 增加停止按钮行为前的测试窗体行为备份。 |
| `.backups\20260908-bst-stop-interval\WanjiStandardTestForm.Designer.cs` | 显式绑定停止按钮事件前的测试窗体 Designer 备份。 |
| `.backups\20260908-bst-stop-interval\WanjiStandardTestCaseHandlers.cs` | BST 千次执行、可取消等待和统一 200 ms 间隔修改前的用例函数备份。 |
| `.backups\20260908-bst-stop-interval\SetMe.ini` | BST 兼容性测试次数更新为 1000 前的配置备份。 |
| `.backups\20260908-bst-stop-interval\项目文件说明.md` | 本次 BST 停止与间隔调整前的项目文件说明备份。 |
| `.backups\20260908-unified-test-stop\WanjiStandardTestForm.cs` | 将停止功能提升为测试大框统一入口前的窗体行为备份。 |
| `.backups\20260908-unified-test-stop\WanjiStandardTestCaseHandlers.cs` | BST 接入大框统一取消令牌前的用例函数备份。 |
| `.backups\20260908-unified-test-stop\DesktopCommService.cs` | 版本读取各协议步骤接入停止检查前的服务层备份。 |
| `.backups\20260908-unified-test-stop\项目文件说明.md` | 本次统一停止功能修改前的项目文件说明备份。 |
| `.backups\20260908-eventreport-one-way\TestResultFileWriter.cs` | 删除 EventReport 响应占位日志前的结果写入器备份。 |
| `.backups\20260908-eventreport-one-way\WanjiStandardTestForm.cs` | 修正 BST EventReport 逻辑说明前的窗体行为备份。 |
| `.backups\20260908-eventreport-one-way\dsrc-csharp-host-developer.SKILL.md` | 增加 EventReport 单向断链规则前的 skill 备份。 |
| `.backups\20260908-eventreport-one-way\项目文件说明.md` | 本次 EventReport 单向日志修正前的项目文件说明备份。 |
| `.backups\20260908-bst-progress-no-eventreport\WanjiStandardTestCaseHandlers.cs` | 将错误的 CPC 固定 23 帧逻辑纠正为 OBU 随机 BST 流程前的用例函数备份。 |
| `.backups\20260908-bst-progress-no-eventreport\WanjiStandardTestForm.cs` | 更新 OBU BST 兼容性逻辑说明前的窗体行为备份。 |
| `.backups\20260908-bst-progress-no-eventreport\项目文件说明.md` | 本次 OBU BST 来源纠正前的项目文件说明备份。 |
| `.backups\20260908-obu-standard-first-cases\WanjiStandardTestCaseHandlers.cs` | 接入随机避让、防碰撞、预读和 255S 保持测试前的用例函数备份。 |
| `.backups\20260908-obu-standard-first-cases\WanjiStandardTestForm.cs` | 更新首批 OBU 标准测试逻辑弹窗说明前的测试窗体行为备份。 |
| `.backups\20260908-obu-standard-first-cases\项目文件说明.md` | 本次首批 OBU 标准测试流程接入前的项目文件说明备份。 |
| `.backups\20260907-left-station-panel\MainForm.cs` | 增加左侧台发参数通道前的主窗体行为备份。 |
| `.backups\20260907-left-station-panel\MainForm.Designer.cs` | 增加左侧台发参数通道前的设计器备份。 |
| `.backups\20260907-left-station-panel\WJ_DSRCProtocolTest_Desk_Net.csproj` | 接入台发 DLL 和相关源码前的项目文件备份。 |
| `.backups\20260907-port-monitor-log-return\MainForm.cs` | 增加串口实时监测及修正日志返回码前的行为代码备份。 |
| `.backups\20260907-port-monitor-log-return\MainForm.Designer.cs` | 增加串口实时监测控件前的设计器备份。 |
| `.backups\20260907-port-monitor-log-return\DesktopCommService.cs` | 修正 DLL 日志返回码语义前的服务层备份。 |
| `.backups\20260907-transparent-test\MainForm.cs` | 接入透传测试控件前的主窗体行为备份。 |
| `.backups\20260907-transparent-test\MainForm.Designer.cs` | 接入透传测试控件前的主窗体设计器备份。 |
| `.backups\20260907-transparent-test\WJ_DSRCProtocolTest_Desk_Net.csproj` | 登记透传测试源码前的项目文件备份。 |
| `.backups\20260907-transparent-test\DesktopCommNativeMethods.cs` | 增加透传 DLL 导出声明前的 Native 层备份。 |
| `.backups\20260907-transparent-test\DesktopCommService.cs` | 增加透传命令路由和初始化状态前的服务层备份。 |
| `.backups\20260907-transparent-test\项目文件说明.md` | 本次透传功能开发前的项目文件说明备份。 |
| `.backups\20260907-transparent-layout-correction\TransparentTestControl.cs` | 按用户说明纠正实时帧、人工帧和解析区职责前的透传行为备份。 |
| `.backups\20260907-transparent-layout-correction\TransparentTestControl.Designer.cs` | 纠正透传三区布局前的 Designer 备份。 |
| `.backups\20260907-transparent-layout-correction\项目文件说明.md` | 本次透传布局纠正前的项目文件说明备份。 |
| `.backups\20260907-transparent-layout-buttons\TransparentTestControl.cs` | 调整实时/人工帧按钮位置并移除快捷组帧与超时选择前的行为备份。 |
| `.backups\20260907-transparent-layout-buttons\TransparentTestControl.Designer.cs` | 本次透传列表拉长和按钮重排前的 Designer 备份。 |
| `.backups\20260907-transparent-layout-buttons\项目文件说明.md` | 本次透传布局调整前的项目文件说明备份。 |
| `.backups\20260907-designer-foreach-fix\TransparentTestControl.Designer.cs` | 修复 WinForms 设计器不支持 `foreach` 语句前的透传控件 Designer 备份。 |
| `.backups\20260907-designer-foreach-fix\项目文件说明.md` | 修复 Designer 加载错误前的项目文件说明备份。 |
| `.backups\20260907-buf-test-layout\TransparentTestControl.Designer.cs` | 调整 BUF 高度及“修改”“清空 BUF”“测试”按钮位置前、包含用户手动布局修改的 eSafeNet 原始 Designer 备份。 |
| `.backups\20260907-buf-test-layout\项目文件说明.md` | 本次 BUF 与测试按钮布局调整前的项目文件说明备份。 |

## Visual Studio 本地状态目录

`.vs\` 由 Visual Studio 自动生成，只保存当前计算机上的工作区状态，不属于程序源码，不应手工修改或作为业务功能依赖。

| 路径或类型 | 功能 |
| --- | --- |
| `.vs\WJ_DSRCProtocolTest_Desk_Net\FileContentIndex\*.vsidx` | Visual Studio 文件内容索引缓存；当前包含多个以 GUID 命名的 `.vsidx` 文件。 |
| `.vs\WJ_DSRCProtocolTest_Desk_Net\v18\.suo` | 当前用户的 Visual Studio 解决方案选项和本地状态。 |
| `.vs\WJ_DSRCProtocolTest_Desk_Net\v18\DocumentLayout.json` | 当前编辑器文档布局。 |
| `.vs\WJ_DSRCProtocolTest_Desk_Net\v18\DocumentLayout.backup.json` | Visual Studio 自动保存的文档布局备份。 |

## 构建输出目录

`bin\Debug\` 和 `bin\Release\` 是最终构建输出，均由 MSBuild 生成或复制。Debug 用于调试，Release 用于发布验证。这里的文件不应直接编辑；修改应回到源码或 `Native` 依赖后重新构建。

| 文件模式或当前文件 | 功能 |
| --- | --- |
| `bin\Debug\WJ_DSRCProtocolTest_Desk_Net.exe` | Debug 版 x86 上位机可执行程序。 |
| `bin\Release\WJ_DSRCProtocolTest_Desk_Net.exe` | Release 版 x86 上位机可执行程序。 |
| `bin\Debug\WJ_DSRCProtocolTest_Desk_Net.pdb`、`bin\Release\WJ_DSRCProtocolTest_Desk_Net.pdb` | 对应构建的调试符号，用于定位源码行。 |
| `bin\Debug\DeskTopComm.dll`、`bin\Release\DeskTopComm.dll` | 构建时从 `Native\DeskTopComm.dll` 复制的运行依赖。 |
| `bin\Debug\DLL_log_2026- 9- 7.txt` | `DeskTopComm.dll` 在 Debug 程序运行期间生成的 DLL 日志；属于运行产物，可用于通信问题诊断。 |
| `bin\Debug\TestResults\`、`bin\Release\TestResults\` | 测试运行时按用例生成的结果目录；每个结果文件包含最终结论、版本号、DLL 返回码和完整请求/响应帧。目录不参与编译，不应手工修改。 |
| `bin\Debug\TestScreenshots\`、`bin\Release\TestScreenshots\` | 勾选“存储测试结果”并执行任意测试用例时生成的测试大框 PNG 截图目录。目录不参与编译，不应手工修改。 |

## 中间构建目录

`obj\x86\Debug\` 和 `obj\x86\Release\` 完全由 MSBuild/Visual Studio 生成，不属于手工源码。文件可能随构建增加、重建或消失，因此按类型说明。

| 文件模式 | 功能 |
| --- | --- |
| `.NETFramework,Version=v4.8.AssemblyAttributes.cs` | MSBuild 自动生成的目标框架程序集属性。 |
| `WJ_DSRCProtocolTest_Desk_Net.exe` | 编译阶段产生、随后复制到 `bin` 的中间可执行文件。 |
| `WJ_DSRCProtocolTest_Desk_Net.pdb` | 编译阶段产生的中间调试符号。 |
| `WJ_DSRCProtocolTest_Desk_Net.MainForm.resources` | 由 `MainForm.resx` 编译生成的二进制窗体资源。 |
| `WJ_DSRCProtocolTest_Desk_Net.csproj.AssemblyReference.cache` | 项目程序集引用解析缓存。 |
| `WJ_DSRCProtocolTest_Desk_Net.csproj.CoreCompileInputs.cache` | C# 核心编译输入状态缓存。 |
| `WJ_DSRCProtocolTest_Desk_Net.csproj.GenerateResource.cache` | 资源生成输入和结果缓存。 |
| `WJ_DSRCProtocolTest_Desk_Net.csproj.FileListAbsolute.txt` | MSBuild 记录的构建输入/输出绝对路径清单。 |
| `DesignTimeResolveAssemblyReferences.cache` | Debug 设计时程序集引用解析缓存。 |
| `DesignTimeResolveAssemblyReferencesInput.cache` | Debug 设计时引用解析输入缓存。 |

## 当前代码调用关系

```text
Program.cs
  → MainForm.Designer.cs（界面、控件、事件绑定）
  → MainForm.cs（事件行为、串口监测、输入校验、四个独立标准测试入口）
      → Controls\TransparentTestControl.Designer.cs（透传页布局和事件绑定）
      → Controls\TransparentTestControl.cs（帧编辑、校验、执行和显示）
          → Services\DesktopCommService.cs（台发业务与句柄生命周期）
          → Services\TestResultFileWriter.cs（测试结果和完整交互帧文件）
          → Services\TestResultScreenshotWriter.cs（测试大框 PNG 截图）
      → WanjiStandardTestForm.Designer.cs / WanjiStandardTestForm.cs（万集 OBU 标准测试独立大框）
      → BeijingLocalStandardTestForm.Designer.cs / BeijingLocalStandardTestForm.cs（北京地标协议测试独立大框）
      → WanjiCpcStandardTestForm.Designer.cs / WanjiCpcStandardTestForm.cs（万集 CPC 卡标准测试独立大框）
      → WatchmanBroadcastTestForm.Designer.cs / WatchmanBroadcastTestForm.cs（守望者播报测试独立大框）
          → Native\DesktopCommNativeMethods.cs（P/Invoke）
              → Native\DeskTopComm.dll / 输出目录 DeskTopComm.dll
```

## 2026-09-09 OBU 用例 3～10 逻辑核对

- `WanjiStandardTestCaseHandlers.cs`：增加协议响应内容判定；预读三个子项按各自目标文件执行；GetSecure 使用原 OBU 四组请求帧并校验 `15 01` 和全零认证器；TransferChannel 校验 `90 00 00`，第 11/12 项改为三条不同 APDU；SetMMI 恢复 GetSecure 前置步骤和 3 秒人工观察时间；防碰撞增加五组计时读取及 8 ms 判定；255S 配置改读 `BST_Locked_Sutong.ini`。
- `WanjiStandardTestForm.cs`：双击防碰撞、预读、信道、GetSecure、TransferChannel、SetMMI、拼帧和 255S 用例名称时，显示从原 OBU C++ 实现整理出的执行流程与通过标准，并明确自动判据和人工判据的边界。
- 本轮未修改任何 `.Designer.cs` 或 `.resx`；构建验证输出位于 `.verification\obu-cases-3-10\`。

## 2026-09-09 OBU 版本号详情匹配修复

- `WanjiStandardTestForm.cs`：版本号读取详情由旧的叶子名称精确比较改为完整树路径末尾比较，修复采用完整节点路径防重名后，双击“0、OBU版本号读取”错误落入占位说明的问题。
- 本次只修改详情匹配行为，未修改版本号通信流程、设计器或资源文件。

## 2026-09-09 双显示器 DPI 适配

- `App.config`：启用 .NET Framework 4.8 WinForms 的高 DPI 自动调整和 DPI 变化消息处理。
- `app.manifest`：声明 `PerMonitorV2`，使程序从外接显示器移动到笔记本屏幕时按目标显示器缩放比例重新布局。
- `WJ_DSRCProtocolTest_Desk_Net.csproj`：登记应用配置和 DPI 清单文件，确保构建时写入可执行程序。
- `Controls\TransparentTestControl.Designer.cs`：扩大上下行帧、帧操作和 BUF 右侧按钮列，将右侧上下区域调整为30%/70%，给帧操作的四个可伸缩按钮行保留更多高度，防止高 DPI 字体被裁切；未修改任何按钮事件或协议行为。

## 2026-09-09 笔记本屏幕控件重叠与文字截断修复

- `MainForm.Designer.cs`：增加通信端口与台发参数区域的可滚动内容高度；复选项行改为145逻辑像素固定高度，避免“启用日志”和初始化按钮重叠；单位列扩大至44逻辑像素，完整显示 `ms` 和“次”。
- `Controls\TransparentTestControl.Designer.cs`：字段标题列扩大至72逻辑像素，完整显示“交易帧”“IC 卡”“ESAM”和“BUF”。
- 左侧容器继续使用滚动条适应较矮的笔记本工作区，不改变台发设置或透传测试行为。

## 2026-09-09 透传操作按钮文字二次适配

- `Controls\TransparentTestControl.Designer.cs`：将“添加”和“测试”所在的两个操作列扩大到96逻辑像素，三个模板操作行均提高到40逻辑像素，并让添加按钮填充单元格，解决笔记本高 DPI 下“测试”显示省略号及添加按钮字形下沿被裁切的问题。

## 2026-09-09 测试用例详情全量匹配校验

- `WanjiStandardTestForm.cs`：BST兼容性详情改为按完整树路径末尾匹配，修复双击后误入通用占位说明的问题。
- 已接入执行逻辑的32个测试入口逐项校验，均能返回对应流程和判定说明，不存在空详情或误入占位详情。
- 尚未实现协议逻辑的预留用例改为显示完整测试路径和明确的“尚未实现”状态，不虚构数据帧、测试流程或通过结果。
- GetSecure、TransferChannel、SetMMI 和拼帧详情改为按所属组编号匹配，避免后续预留用例仅因名称中出现相同协议词而误显示其他组详情。

更新日期：2026-09-09。
2026-09-11 测试信息显示精简：`WanjiStandardTestCaseHandlers.cs` 的 BST 兼容性、通用协议序列和 VST 随机避让界面日志不再显示完整请求/响应帧、DLL 返回码等底层信息，只显示测试进度、协议步骤、判定说明和最终结果；`Services\LaneTransactionService.cs` 的车道交易步骤日志同步改为业务步骤“完成/失败”。完整交互帧及底层返回码仍由 `Services\TestResultFileWriter.cs` 在勾选“存储测试结果”后写入 exe 同级 `TestResults\` 文档。未修改任何 Designer 或 `.resx`。修改前副本位于 `.backups\20260911-test-information-summary\`。
2026-09-11 BST 原位进度与车道交易空中帧显示：`WanjiStandardTestCaseHandlers.cs` 将 BST 兼容性测试进度改为 RichTextBox 固定位置原位更新，按每轮实际完成的通信次数显示 `已测试次数/配置总次数`，例如 `1/1000`、`2/1000`，不再逐条追加进度和成功日志；`Services\LaneTransactionService.cs` 作为本次明确指定的例外，在每个车道交易步骤中显示下行发送和上行接收的完整空中帧、方向及长度，EventReport 仍只显示单向下行帧。其他测试用例继续保持精简显示。未修改 Designer 或 `.resx`。修改前副本位于 `.backups\20260911-bst-progress-lane-airframes\`。
2026-09-11 车道交易进度与失败回复分析：`WanjiStandardTestCaseHandlers.cs` 将车道交易测试改为固定位置显示“成功次数/配置总次数”，每轮完整流程通过后才递增，不再逐轮追加成功信息；`Services\LaneTransactionService.cs` 取消成功步骤的空中帧输出，仅在失败时显示具体失败步骤、该步骤下行请求、OBU 上行回复和中文分析。TransferChannel 类回复的成功判定同步收紧为末尾严格匹配 `90 00 00`；未发送、超时/无回复、回复过短、状态码不符和 VST 数据结构不符分别给出原因。所有交互帧仍完整进入勾选后的结果文档。未修改 Designer 或 `.resx`。修改前副本位于 `.backups\20260911-lane-progress-failure-analysis\`。
2026-09-11 TransferChannel 多 APDU 逐项判定：`Services\LaneTransactionService.cs` 对照原上位机 `CODE_ENCODE.cpp::DecodeTransferChannel_Rq/Rs`，解析请求 `APDUList`、每条 APDU 长度、回复 `DataList`、每条 Data 长度和最终 `ReturnStatus`。车道交易中任意一条 Data 的末两字节不是 `90 00` 即失败，界面指出第几条指令、该 APDU 内容和实际响应码；同时校验请求/回复数量一致及 `ReturnStatus=00`，不再只检查完整回复帧末尾。修改前副本位于 `.backups\20260911-transfer-multi-apdu-validation\`。
2026-09-11 ESAM 状态码中文判断：`Services\LaneTransactionService.cs` 新增独立 `DescribeEsamStatusCode` 函数，依据用户提供的《ESAM技术手册（多逻辑通道）V1.1》集中解释 `63Cx`、`65 81`、`66 00`、`67 00`、`69xx`、`6Axx`、`6B 00`、`6Cxx`、`6D 00`、`6E 00`、`6F 00`、`93xx`、`94xx` 等状态码。车道交易的多 APDU 异常显示统一调用该函数，输出实际响应码及中文原因；未知码明确提示手册未定义。修改前副本位于 `.backups\20260911-esam-status-code-descriptions\`。
2026-09-11 TransferChannel 多错误汇总显示：`Services\LaneTransactionService.cs` 的多 APDU 回复校验不再遇到首个错误码即返回；在回复长度边界仍可解析的情况下遍历全部 Data，按顺序汇总每个异常的指令序号、对应下发 APDU、实际响应码和 ESAM 中文原因，并同时报告指令数量不一致及非零 ReturnStatus。仅当长度字段损坏导致后续 Data 边界无法确定时停止继续解析。修改前副本位于 `.backups\20260911-transfer-all-status-errors\`。
2026-09-11 测试树多选顺序执行与父子联动：`WJ_DSRCProtocolTest_Desk_Net\WanjiStandardTestForm.cs` 允许同时勾选多个叶子测试用例，并按测试树从上到下的显示顺序加入现有执行队列；勾选或取消勾选父级节点时，其全部子孙节点同步勾选或取消。目标收集只加入叶子节点，避免父级和子级重复执行。`WJ_DSRCProtocolTest_Desk_Net\WanjiStandardTestForm.Designer.cs` 显式绑定 `AfterCheck` 事件。修改前副本位于 `.backups\20260911-multi-select-tree-check\`。
2026-09-11 测试信息自动滚动与 VST 三窗唯一判定：`WJ_DSRCProtocolTest_Desk_Net\WanjiStandardTestForm.Designer.cs` 为测试信息框显式绑定 `TextChanged`，`WanjiStandardTestForm.cs` 在文本追加或原位更新后自动滚动到末行，并同步修订随机避让用例详情。`WanjiStandardTestCaseHandlers.cs` 的 VST 随机避让不再以 BST、EventReport、采样通信状态或完成轮数作为独立失败条件，通信异常继续下一轮，最终结果只比较三个目标时间窗的累计数量是否分别严格大于配置次数的 1/4。修改前副本位于 `.backups\20260911-autoscroll-vst-window-only\`。
2026-09-11 全部用例统一测试结论：`WJ_DSRCProtocolTest_Desk_Net\WanjiStandardTestForm.cs` 在所有测试用例共用的结果持久化入口中，无论是否勾选“存储测试结果”，均先在测试信息框显示“最终测试结果：测试成功”或“最终测试结果：测试失败”。`Services\TestResultFileWriter.cs` 与 `Services\TestResultScreenshotWriter.cs` 同步使用“测试成功/测试失败”生成 TXT、PNG 文件名，TXT 内容中的最终结论也使用相同字样。修改前副本位于 `.backups\20260911-unified-test-result-words\`。
2026-09-14 备用门架交易测试：`Services\LaneTransactionService.cs` 新增旧版 `WJ_MastTrade` 对应的门架 BST/VST、GetSecure、ICC、ESAM EF04、门架专用 EF04 写入、消费初始化、40 字节扣费和单向 EventReport 流程；旧版扣费后不追加 SetMMI；`WanjiStandardTestCaseHandlers.cs` 将“备用测试项 > 2、门架交易测试”接入现有分发表、后台取消、进度和结果保存机制；`WanjiStandardTestForm.cs` 增加该用例的流程/判定说明。门架交易次数和间隔沿用 `[SET_Trade_GB]`；对照原 `SMARTETC_TRADE` 配置加载与 `ConsumeInitialize_MJ_ruanpsam` 确认，门架扣费金额读取 `[TRADE_SET]/19` 的前 4 字节，配置末字节不用于门架，门架交易状态按原函数固定为 `0x03`（车道交易仍使用配置末字节）；报文布局兼容旧版 `WJ_MastTrade`，但算法按界面选择：3DES 的消费初始化算法标识为 `0x01`，SM4 为 `0x41`，MAC1 使用对应软算法；门架消费初始化的 6 字节终端交易序号按原函数覆盖为 `37 37 37 37 37 37`，保证与 MAC1 原文一致，避免扣费返回 `93 02`；失败出口补发单向 EventReport 释放会话。真实 RSU/OBU 交互仍需实机确认。
## 2026-09-14 典型交易测试迁移

- `Services/LaneTransactionService.Typical.cs`：按原 `WJ_Trade_typicaltransfer` 与 `WJ_Trade_typicaltransfer_SM4` 的 switch 结构实现备用测试项“3、典型交易测试”，固定执行 6 个协议组合分支。
- `Services/LaneTransactionService.cs`：改为 partial，继续提供共用通信、VST 解析、状态校验和已有车道/门架帧构造逻辑。
- `Services/SoftTradeCryptoService.cs`：增加原双片式 SM4 典型交易外部认证数据计算。
- `WanjiStandardTestCaseHandlers.cs`：增加典型交易分发、后台执行、进度显示及结果文件保存。
- `WanjiStandardTestForm.cs`：增加典型交易测试说明和逐字段参数来源说明。
- 关键参数来源：金额为 `SetMe.ini [TRADE_SET]/19` 前 4 字节；典型交易的 0019 出入口状态按原函数固定为 `04`；MACID/ICC0015 来自本轮 VST；EP/随机数来自本轮消费初始化回复；UTC 交易时间运行时生成；终端机编号为原模板固定的六个 `37`；算法由界面选择，SM4/3DES 标识分别为 `41/01`。

## 2026-09-14 测试截图命名与父级汇总

- `Services/TestResultScreenshotWriter.cs`：截图文件名调整为“去编号用例名称_成功或失败_yyyyMMdd_HHmmss_万集.png”，时间精确到秒。
- `WanjiStandardTestForm.cs`：执行多个子叶子的父级测试时，子叶子继续分别保存 TXT 结果但不逐项截图；全部子叶子结束或中途停止后，以共同父级名称和汇总状态仅保存一张最终截图。

## 2026-09-14 预读信息测试校对

- `WanjiStandardTestCaseHandlers.cs`：依据原 `WJ_Trade_PreReadInfomation` 校正三个预读用例。公共下发顺序为预读 BST、选择 ESAM 3F00、读取 ESAM EF01、选择 IC 1001、读取目标 IC 文件、单向 EventReport；LLC 按 `F7/77` 交替。
- 用例1的 0002 实读指令修正为余额 APDU `80 5C 00 02 04`；用例3的 0012 读取长度修正为 `24`（36字节）。
- 成功判据恢复为原逻辑：VST 偏移14的26字节 ESAM 系统信息必须等于 EF01 实读内容；VST 偏移41的目标文件预读数据必须等于后续实读内容；用例1还比较 VST 偏移84的4字节余额。
2026-09-14 交易过程中响应不同 MAC 流程校正：重新核对原 `WJ_Trade_RespondDiffMac` 后，`Services/DesktopCommService.cs` 将用例显式会话 MAC 写入与台发“MAC 自动更新”开关解耦。三个子用例均先用 VST 返回的原 MAC 发送 GetSecure；仅在 GetSecure 成功后的 TransferChannel 中分别使用末字节加一、全 `FF`、第三字节加一的错误 MAC，并以 OBU 不响应为通过条件。`WanjiStandardTestCaseHandlers.cs` 同步明确该调用约束。
2026-09-15 万集标准化测试全量审计启动：新增 `万集标准化测试用例校验.md`，按原上位机入口函数、初始化参数、BST/VST、MAC、LLC、帧长、响应判据、时序和恢复动作记录93个叶子用例的覆盖状态。审计确认指令集19～28不能使用单APDU通用模板，6984两项包含两次独立BST会话；同时记录现有指令集1～17仍需校正的完整前置和LLC轮换。
2026-09-15 指令集响应码用例19～28迁移：`WanjiStandardTestCaseHandlers.cs` 对照原 `WJ_Trade_instruction` 将十个占位入口替换为实际协议流程，恢复 3F00/DF01 目录前置、ICC/ESAM 信道、随机数前置、同帧多 APDU、逐帧 `F7/77` LLC 轮换以及 `6982/6981/6A83/6984/6986/6988/6B00/6F00/6985` 三字节响应判定；结束统一发送 SetMMI 和单向 EventReport。按用户指定，顶层编号0～11不纳入后续复查范围。
2026-09-15 不同MAC实机修正：`Services/DesktopCommService.cs` 新增仅切换 MAC 自动更新并保留其余初始化参数的重初始化入口；`WanjiStandardTestCaseHandlers.cs` 在顶层12三个不同MAC负向用例期间临时关闭 DLL 的 MAC 自动更新，确保第一个子项 TransferChannel 的 MAC 末字节加一、第二项全FF、第三项第三字节加一不会在发射前被 VST MAC 覆盖，用例结束后恢复用户原配置。
2026-09-15 透明测试 Transfer 4：`Controls/TransparentTestControl.cs` 在交易帧下拉模板的 Transfer 3 下方新增 Transfer 4，使用用户指定的 ESAM 信道五 APDU TransferChannel 完整帧；沿用现有“添加”与“测试”按钮逻辑，不修改设计器布局和事件绑定。
2026-09-15 透明测试 LLC 队列更新：`Controls/TransparentTestControl.cs` 在勾选主界面“LLC 更新”时，于发送前按待测列表显式生成 GetSecure、TransferChannel、SetMMI 的 `F7/77` 交替值，BST 作为新会话重置轮换，并把实际值回写到列表和下行日志；未勾选时保持用户输入。`Services/DesktopCommService.cs` 新增仅切换 LLC 更新的重初始化入口，显式组帧期间临时关闭 DLL 二次覆盖，测试结束恢复原配置。
2026-09-15 单帧唤醒测试迁移：`WanjiStandardTestCaseHandlers.cs` 与 `Services/DesktopCommService.cs` 对照原 `WJ_ProtocolTest_SingleFrame` 与 `ETC.cpp::SendBST_GB`，顶层16读取 exe 同级 `SetMe.ini [SET_DANZHEN]/number`（缺失时30次），测试前以 BST=90、重发间隔=20、重发次数=32、功率=10、信道=0、超时=80ms 重初始化台发，同时保留用户原 BID/LLC/UnixTime/MAC 更新开关；每轮使用原28字节BST且间隔2000ms。BST请求失败立即结束，VST失败继续后续轮次，最终仅全部轮次收到有效VST时通过；结束后恢复测试前完整台发配置。
2026-09-15 模拟真实交易流测试迁移：`WanjiStandardTestCaseHandlers.cs`、`Services/DesktopCommService.cs` 与 `Services/LaneTransactionService.cs` 对照原 `WJ_TransactionFlow_ruansuan`，顶层17从占位入口替换为软算法连续交易流。每轮读取 `[SET_Trade_GB]/number`、`interval` 及 `[TRADE_SET]/19`，并按旧固定专用初始化（BST=30、功率=10、信道0、BID更新开、UnixTime更新关、MAC更新开，保留用户LLC/重发/超时）。轮内按入口零金额状态03交易、500ms后同BID BST无VST、门架交易、断链后30次门架BST/VST、出口配置金额交易、500ms后BST无VST的顺序执行；入口、门架、出口各自沿用现有软算MAC、EF04、SetMMI/EventReport流程。任一阶段失败即结束，成功数必须等于配置轮数；结束后恢复原台发配置。真实设备上的BID保持与帧模板差异仍需实机确认。
2026-09-15 万集自有协议55AA迁移：`WanjiStandardTestCaseHandlers.cs` 对照原 `WJ_RFSendDataTest`，将顶层18第6项替换为实际73通道测试；轮次读取`[SET_SEND_RF]/number`，每轮BST/VST后固定执行50对01/02指令，分别校验响应偏移14起108字节严格交替为`55 AA`和`AA 55`并校验`90 00 00`，随后发送SetMMI并按交易间隔等待。
2026-09-15 Transfer 4 临时 -100 兼容：`Controls/TransparentTestControl.cs` 仅对固定 Transfer 4 的 APDU 内容增加临时放行。当 DLL 请求返回0、响应返回-100时记录“临时兼容”日志并继续后续透明帧；其他帧、其他错误码及标准化测试仍按原成功判据处理。该判断带 TODO，待 DLL 多 APDU 返回码问题确认后删除。
2026-09-15 VST EquipmentStatus用例迁移：`Services/DesktopCommService.cs` 与 `WanjiStandardTestCaseHandlers.cs` 对照原 `WJ_VstEquipmentclass_VersionTest`，恢复 SendBST_GB帧、VST EquipmentInfo提取、71通道两步动态认证、70通道`70 01 01 DD`读取以及单向EventReport；测试信息区显示十六进制状态并保存完整交互帧。
2026-09-15 万集自有协议蓝牙地址读取迁移：对照原 `WJ_MPoint_BLEMAC`，恢复SendBST_GB、71通道两步认证、70通道`70 01 01 75`读取、响应偏移14的6字节地址提取、SetMMI及单向EventReport；地址和完整交互帧进入统一测试结果。
2026-09-15 万集自有协议MACID/SN修改迁移：对照原 `WJ_70_AmendMACID` 与 `WJ_70_AmendSN`，次数读取`SetMe.ini [SET_AmendMACIDSN]/number`；每轮重新BST/VST和71认证，随机字节范围保持`00～59`。MACID发送`70 01 05 71`并校验状态，不额外发送SetMMI；SN发送`70 01 09 D4`后通过`70 01 01 D9`复读8字节严格核对并发送SetMMI；两者轮间均等待2秒。
2026-09-15 万集自有协议地区/通道用例迁移：对照原 `WJ_MPoint_Area` 和 `WJ_ChannelValidation`。地区用例保持旧代码最终行为，以`70 01 01 DD`实读值覆盖INI地区值，再用`70 01 02 DC`原值回写并DD复读核对；通道用例读取`SetMe.ini [SET_Channel]/Channel`并形成`70 01 01 xx`。两项均包含71认证、SetMMI和单向EventReport。
2026-09-15 拼帧交易测试迁移及播报状态校正：`Services/LaneTransactionService.cs` 对照旧 `TEST_PROTOCOL.cpp::WJ_FrameConcateTest` 新增同链路双交易流程。外层轮次和轮间等待分别读取 `[SET_Trade_GB]/number`、`interval`，两笔金额严格读取 `[ConcateFrameTrade]/tradeCount1`、`tradeCount2`（单位为分）；每轮仅执行一次BST/VST、GetSecure和ICC读取，然后按LLC连续轮换依次执行两组消费初始化/MAC1软算/Consume，最后执行一次SetMMI和单向EventReport。配置缺失或金额非法时明确失败，不使用猜测默认值。同步复核旧 `TEST_ONECHIP_TRADE.cpp::WjConsumeInitialize` 后，本用例及播报金额用例均使用专用79字节模板：六字节终端机编号固定为`37`、UnixTime固定为`1A A1 DD 02`、交易状态固定为`04`（ETC出口），不再套用普通车道交易的动态时间。播报金额仍使用原用例9档固定金额和8000 ms间隔。`WanjiStandardTestCaseHandlers.cs` 将通用测试项20接入后台取消、进度和结果保存。未修改Designer、资源、INI或DLL；真实拼帧交易与语音效果仍需实机确认。
2026-09-15 播报金额期望内容复核：`Services/LaneTransactionService.cs` 对照原 `WJ_AmountAnnouncementTest`，九档测试日志改为原界面逐字内容，不再按金额重构中文读法：0.01、0.16、5.62、20、22.32、100、1001.62、4000元分别对应“ETC消费零点零一元、零点一六元、五点六二元、二十元、二十二点三二元、一百元、一千零一点六二元、四千元”；4000.01元为“标签应不播报，仅蜂鸣”。这些文本只用于人工语音核验，不改变交易帧金额、协议成功判据或轮间8000ms等待。
2026-09-15 拼帧交易播报金额显示：`Services/LaneTransactionService.cs` 在拼帧交易配置日志中保留 `[ConcateFrameTrade]/tradeCount1` 与 `tradeCount2` 两笔金额，同时以分为单位精确相加并显示“最终播报金额”；该值仅供人工核验拼帧后的播报总额，不改变两笔独立扣费帧、状态04或成功判据。

2026-09-15 播报金额测试迁移：`Services/LaneTransactionService.cs` 对照旧 `TEST_PROTOCOL.cpp::WJ_AmountAnnouncementTest` 新增九档专用软算交易，金额由旧用例固定表直接提供，依次为 1、16、562、2000、2232、10000、100162、400000、400001 分（0.01～4000.01 元），不读取 `[TRADE_SET]/19` 的金额；交易状态按旧 `WjConsumeInitialize` 固定为出口 `04`。每档严格执行 BST/VST、GetSecure、ICC 0019/0002、消费初始化并更新0019、软算MAC1、Consume、SetMMI、单向EventReport，且不插入普通车道交易的EF04奇偶轮读写，档位间固定等待8000 ms。`WanjiStandardTestCaseHandlers.cs` 将通用测试项19接入后台取消、进度和结果保存；协议成功后提示人工核对九档语音，4000.01元的旧用例预期为“金额过大”。未修改Designer、资源、INI或DLL；真实语音效果仍需连接RSU/OBU实机确认。
2026-09-15 交易后PIN认证异常播报测试迁移：`Services/LaneTransactionService.cs` 对照旧 `TEST_PROTOCOL.cpp::WJ_TradePinRead0018Test`，固定执行一轮同链路流程：BST/VST、GetSecure、ICC读取、按 `[ConcateFrameTrade]/tradeCount1` 与 `tradeCount2` 连续两次消费初始化/软算MAC1/扣费，随后按下一次LLC下发原49字节组合帧。该帧第一条APDU使用错误PIN `88 88 88 00 00 00`，后接 `00 B2 01/02/03/04 C4 17` 四条0018记录读取；仅当完整回复帧尾严格为 `69 82 00` 时通过，再发送SetMMI和单向EventReport。`WanjiStandardTestCaseHandlers.cs` 将通用测试项21接入后台取消、固定1轮进度、完整帧结果保存，并提示人工确认异常语音。未修改Designer、资源、INI或DLL；真实异常语音仍需实机确认。

2026-09-15 交易25次后防拆2S失效测试迁移：`Services/LaneTransactionService.cs` 对照旧 `TEST_JIAOYI.cpp::WJ_Trade25Rem2sTest` 与 `ETC.cpp::ConsumeInitialize_GB_ruanpsam`，固定执行27轮完整软算车道交易（前25轮累计、后2轮观察），每轮保持原EF04奇偶读写分支并在末尾等待 `[SET_Trade_GB]/interval`。消费金额及出入口状态严格取自 `SetMe.ini [TRADE_SET]/19` 的前4字节和末1字节，算法跟随界面SM4/3DES选择；消费初始化序号按旧全局 `trade_num_25times` 从`00`开始逐轮递增至`1A`，不再误用从1开始的显示轮次。旧配置 `onTradeBst` 默认关闭，现用例同样不追加交易后连续BST。`WanjiStandardTestCaseHandlers.cs` 将通用测试项22接入后台取消、27轮进度和统一结果保存；27轮协议成功后仍提示人工确认第25次后首次防拆失效为2秒、后续恢复200毫秒。未修改Designer、资源、INI或DLL；防拆时长需连接RSU/OBU实机观察确认。

2026-09-15 不同MAC第二、第三子项修复：`Services/DesktopCommService.cs` 与 `WanjiStandardTestCaseHandlers.cs` 将“交易过程中响应不同MAC测试”改为旧 `WJ_Trade_RespondDiffMac` 的专用台发模式。三个子项执行前均完整重初始化为`BST=30、重发间隔/次数=10、功率=10、信道=0、LLC=开、UnixTime=关、MAC=关、超时=500ms`，BID更新状态遵循进入用例前的台发配置；结束后恢复用户原有的全部初始化参数。`DesktopCommService` 的BeaconID改为模拟旧全局`gBeaconID`，当BID更新启用时跨三个子项连续递增，不再每次从BST模板固定字节重新计算；界面显示实际下发的BST BeaconID。GetSecure继续使用本轮VST原MAC，TransferChannel分别使用末字节加一、`FF FF FF FF`、第三字节加一，并以请求成功且OBU接收超时/无回复为通过条件。负向Transfer步骤新增实际传入DLL的四字节MAC显示，便于从界面和结果记录确认第二、第三项未被覆盖。未修改Designer、资源、INI或DLL；实际空口无响应仍需RSU/OBU实机验证。
2026-09-15 指令集子用例间隔统一：`WanjiStandardTestCaseHandlers.cs` 对照旧 `WJ_Trade_instruction` 在每个子用例发送单向 EventReport 后的 `Sleep(1000)`，新增统一常量并将指令集第1～17项与响应码第19～28项都固定为 1000 ms。此前响应码用例第19～28项错误等待 2000 ms；现父级顺序执行时下一子项 BST 会在前一子项断链后约1秒再发送。未修改同一子用例内部的 BST、TransferChannel、SetMMI 帧间时序、Designer、INI 或 DLL。
2026-09-15 提前失败断链补偿：`WanjiStandardTestCaseHandlers.cs` 筛查标准化测试步骤定义后确认，正常路径均以单向 EventReport 结束；问题出在前置通信或响应校验失败时通用序列执行器直接返回，跳过了末尾断链。现对任何配置了末尾 EventReport 的协议序列增加补发：若尚未实际执行 EventReport，则从当前用例步骤取得原有 EventReport 负载和超时并发送，记录至结果交互帧；正常路径不重复发送。补发动作不等待 OBU 回复，也不覆盖原用例失败结论。
2026-09-15 指令集用例10状态码校正：`WanjiStandardTestCaseHandlers.cs` 对照旧 `WJ_Trade_instruction` 的 Le 长度错误分支，将“发送取芯片序列号指令长度不正确”的成功判据由错误的`67 00 00`改为`6C 04 00`。下发 APDU 保持`80 F6 00 03 05`不变；`04`为旧 OBU 指示的正确 Le。未修改其目录前置、LLC、断链、间隔、Designer、INI或DLL。
2026-09-15 保留文件读写测试迁移：`WanjiStandardTestCaseHandlers.cs` 将通用测试项15从占位入口替换为旧 `WJ_SuTong_ESAMfile` 的实际链路：30字节 BST/VST、GetSecure、选择 ICC `1001`、读取 ICC `0009` 保留文件10字节、写入固定数据`00 11 22 33 44 55 66 77 88 99`、SetMMI和单向EventReport。选择、读取、写入均严格判定`90 00 00`；按旧逻辑不复读写入结果，也不恢复原0009内容。未修改Designer、INI或DLL，实机执行会永久改写该保留文件的前10字节，需使用可测试OBU。
2026-09-15 单片式双通道切换测试迁移：`WanjiStandardTestCaseHandlers.cs` 将单片式专用测试项2的四个叶子从占位入口替换为旧 `WJ_Trade_DoubleChannelChange` 的独立会话。每项均使用旧24字节BST/VST与GetSecure，并按旧帧顺序、LLC轮换 `F7/77/F7/77` 下发两条单APDU列表的 TransferChannel：ESAM `3F00/EF01`、ICC `3F00/0016`、ESAM `DF01/EF04`、ICC `1001/0015` 的组合顺序分别与四个界面子项一致。两条Transfer均严格判定完整回复末尾`90 00 00`，随后发送SetMMI和单向EventReport并等待1秒；失败路径由统一执行器补发EventReport。未修改Designer、INI或DLL；旧程序对SetMMI回复只记录不判失败，而新程序按现有统一序列规则同时要求其请求/响应成功，实际兼容性待RSU/OBU实机验证。

2026-09-15 单片式发行与激活测试迁移：新增 `Services/OneChipIssueActivationService.cs`，将单片式一次发行、二次发行和标签激活三个叶子接入真实5.8G流程。一次发行严格读取 exe 同级 `SetMe.ini [SET_Frame]/0015、EF01`，SM4版本字节按旧逻辑置为`52`；根密钥固定沿用旧程序`46 A1 A0 3B A7 2A 63 F6 F4 B8 C0 6D 0F 0B 6D 6C`，按旧业务代码、配置发行方、芯片序列号或合同/卡内部序列号执行三级SM4分散。发行前实读ESAM EF01和ICC 0015并分类：全00/FF按空白密钥处理，自建发行方按实读发行方反算当前主控密钥，`45 54 43 01`正式密钥直接拒绝；ESAM依次替换3F00索引0/1及DF01索引2/3/8/9/10/11/12，ICC依次替换3F00索引0/1及1001索引2/3/13/14/15，随后分别更新EF01和0015。二次发行读取旧拼写`[SET_Frame_second]/seond_0015`、`second_EF01`并沿用`[SET_139_Dodge_num]/number`；激活按旧流程更新EF01防拆字节。`Services/SoftTradeCryptoService.cs` 增加多级SM4分散、MAC及ECB加解密能力；所有Transfer严格判定帧尾`90 00 00`并记录原始交互。未修改原C++、Designer、INI或设备DLL；发行会永久改写密钥和文件，需使用可发行测试OBU进行实机验证。

2026-09-15 单片式一次发行6988修正：重新逐帧核对旧 `WJ_FirstIssue_DoubleChannel`、`EsamGetChipNumber8`、`EsamGetRand_ESAMONE` 和密钥替换函数后，修正两处会直接改变MAC的数据流。芯片序列号步骤恢复为同一ESAM TransferChannel内的两条APDU：`00 A4 00 00 02 3F 00`与`80 F6 00 03 04`，APDUList数量明确为`02`；按DataList结构提取第二条回复的4字节芯片号并复制两遍形成8字节三级分散因子。一次发行全部密钥替换及EF01/0015安全更新均恢复为仅请求4字节随机数`00 84 00 00 04`，本地尾补四个`00`后作为旧 `SM4CalcuMac` 的8字节初值，不再错误请求并使用8字节随机数。ESAM和ICC首次系统主控替换若失败，按旧逻辑重新取随机数并在空白/自建当前主控密钥之间切换后重试；正式密钥仍拒绝发行。Debug|x86构建通过；尚未执行会永久改写密钥的实机发行。

2026-09-15 单片式发行安全APDU长度修正：依据实机下发截图和旧帧模板确认，`Services/OneChipIssueActivationService.cs` 的动态安全更新正文此前直接放在APDUList数量之后，遗漏每项APDU的长度字节。现统一在正文前编码长度，长度字节不参与安全报文MAC：一次发行EF01为`24`、ICC 0015为`1D`，二次发行EF01为`11`、车辆信息为`29`、ICC 0015为`1D`，激活防拆更新为`0A`。密钥替换帧原本已显式包含`29`，加密读取等固定帧原本已包含`0F`，因此不重复添加。一次发行EF01正确结构恢复为`... 18 02 01 24 04 D6 81 00 1F ... MAC4`。

2026-09-15 透明测试VST解析增强：`Controls/TransparentTestControl.cs` 对照旧 `CODE_ENCODE.cpp::DecodeVST` 和 `TRANS_DIALOG.cpp` 的显示顺序，将上行VST从公共头继续细分为 ApplicationContextMark/DID/ApplicationParameter 位图、ESAM系统信息（发行方、合同类型/版本、合同序列号、签署/到期日期、拆卸标志及保留数据）、可选OBU随机数、IC卡预读容器与数据、可选ESAM保留信息、尾部OBU配置MAC、EquipmentInfo和两字节ObuStatus；同时显示MAC一致性、设备类别/硬件版本及IC卡存在、类型、状态、锁定、防拆、电池位。无IC预读时依据固定7字节尾部反推系统信息长度，有IC预读时结合`1D/28`容器标志定位，兼容26字节及带拆卸/保留数据的较长系统信息；短帧或标志不一致时给出解析提示，不修改透传收发流程、帧内容或DLL调用。

2026-09-15 北京地标模块第一、二阶段：新增 `北京地标协议测试用例迁移对照.md`，确认旧“北京地标协议测试”页签实际承载 `ONECHIP_DIALOG`，登记四个一级类别、活动叶子、A～X共24个调度入口、公共INI来源及原版未实现边界。`BeijingLocalStandardTestForm.cs/.Designer.cs` 将占位页改为现架构下的目录与调度框架，支持父子勾选、顺序执行、停止令牌、进度和状态显示；`MainForm.cs` 传入共享 `DesktopCommService`。当前处理器映射刻意保持为空，点击待迁移或原版未实现项只显示审计状态，不调用DLL或生成虚假测试结论。

2026-09-15 透传控件设计器入口修复：`Controls/TransparentTestControl.cs` 在项目中原本已正确声明为 `UserControl`，`.Designer.cs` 和 `.resx` 也已配置 `DependentUpon`；实际阻断 Visual Studio“查看设计器/查看代码”切换的是 `TransparentTestControl.resx` 已变成带 `Esafenet` 头的非XML二进制文件。现将其替换为空资源的标准 WinForms XML `.resx`，未修改控件布局和行为代码；原二进制文件完整保存在 `.backups/20260915-transparent-designer-resx/TransparentTestControl.resx.original`，并额外保留同内容备份 `TransparentTestControl.resx.esafenet-backup`。

2026-09-15 主窗体TabPage设计器异常修复：用户重新打开包含透传页签的 `MainForm` 设计器后，Visual Studio 报 `System.Windows.Forms.TabPage` 空引用。复核确认 `MainForm.resx` 同样是带 `Esafenet` 头的非XML二进制，现替换为不含本地化资源项的最小合法 WinForms XML `.resx`；主窗体全部控件属性仍由 `MainForm.Designer.cs` 管理，未改变运行界面或事件。原资源保存在 `.backups/20260915-mainform-designer-resx/MainForm.resx.original` 和同目录额外备份中。

2026-09-15 OBU标准测试算法默认值：`WanjiStandardTestForm.Designer.cs` 将算法单选项初始化为 SM4（`radioButtonSm4.Checked=true`），3DES 默认不选；用户仍可在界面主动切换算法，所有既有测试入口继续读取当前选中项。

2026-09-15 二次发行加密车辆读取修正：结合17:29实机DLL日志与旧 `WjReadVehicleInfoAndDecrypt` 固定29字节请求确认，加密读取APDU应为`0F 00 B4 00 00 0A + Random8 + 20 + 40`，其中`20`为密钥索引/标识，`40`为SM4密钥版本。新实现此前构造数组少一字节，仅下发`Random8 + 40`，导致声明长度`0F`与实际正文14字节不一致并使用错误密钥读取；现恢复缺失的`20`，完整TransferChannel负载长度由28字节恢复为29字节。回复仍按DataList偏移14提取64字节密文，使用根密钥经`BC D3 C3 DC 00 00 00 03`、VST发行方标识、VST合同序列号三级分散所得密钥解密，并比较明文偏移17起的32字节固定车辆信息。
# 变更记录

2026-09-16 CPC 卡与守望者播报测试框架：两个标准测试窗体参照万集 OBU 标准测试接入测试树、父子勾选、单项/批量执行、停止、清空日志、双击逻辑说明和占位结果显示；真实卡片/播报协议处理器待按原上位机流程逐项移植。
# 2026-09-18 北京地标交易中响应广播帧移植

依据旧工程 `WJ_OneChip_RespondBroadcast` 只读审计，将“OBU基本测试/交易过程中响应广播帧测试”的两个叶子接入北京地标窗体。新增 `BeijingRespondBroadcastService`，按旧程序执行两轮 BST/VST、GetSecure；第二轮增加消费初始化（3DES 参数 `01`，SM4 参数 `41`），随后发送广播 BST 并以无 VST 响应为通过条件，轮间等待 3 秒。结果文件保留完整请求/响应帧和 DLL 返回码。未连接真实设备，未执行实机验证。

同日修正子项划分：旧入口曾把两个子测试连续放在第一个子项中；新上位机已拆分为独立流程。测试用例1只执行 BST/VST、GetSecure、再次广播 BST；测试用例2只执行 BST/VST、GetSecure、消费初始化、再次广播 BST。两项分别生成结果文件。

同日接入“交易过程中响应不同 MAC 测试”：新增 `BeijingRespondDifferentMacService`，三个子项分别使用会话 MAC 末字节加一、全 `FF`、第三字节加一的 TransferChannel，按旧程序以 OBU 无响应为通过条件；测试前进入不同 MAC 模式，结束后恢复原台发初始化配置。未连接真实设备，未执行实机验证。

同日同步北京地标页面的结果保存选项：`BeijingLocalStandardTestForm` 增加与 OBU 标准测试页一致的“存储测试结果”复选框。勾选时保存当前子用例 TXT 和截图，未勾选时只显示界面信息；保存失败只提示，不退出窗体。

同日增加应用启动页：程序启动后先显示三个同级大入口，分别进入 `WJ_DSRC_Protocol_test`、`WJ_OBUSerial_Desk` 和 `WJ_CreatCard_Desk`。其中 `WJ_DSRC_Protocol_test` 复用现有 `MainForm` 的台发通道设置和 DSRC 测试内容；另外两个页面先提供空白功能占位界面，后续在当前工程内继续扩展，不调用外部 EXE。

同日按用户确认撤销启动页按钮导航方案：删除 `LauncherForm` 和共用占位窗体，恢复 `MainForm` 为程序入口；在 `MainForm` 顶部改为三个同级产品 Tab，分别为 `WJ_DSRC_Protocol_test`、`WJ_OBUSerial_Desk` 和 `WJ_CreatCard_Desk`。DSRC Tab 内保留原台发设置、透传模式、标准测试和日常使用内容，后两个 Tab 为当前工程内的独立空白预留页。程序集和输出 EXE 重命名为 `OBU_Comrehensive_Test_Desk`，协议帧流程和测试逻辑未改动。

后续修正命名：程序集、产品标题、主窗体标题和输出 EXE 与项目名称统一为 `WJ_DSRCProtocolTest_Desk_Net`；三个产品 Tab 名称和页面结构保持不变。

同日优化主窗体一级产品 Tab：尺寸从 `250×42` 缩小为 `175×32`，采用固定宽度自绘标签；DSRC、OBU 串口和发卡页面分别使用蓝、绿、橙配色，选中时显示深色背景和白色文字。未修改设备、协议或测试逻辑。

同日再次压缩一级产品 Tab：调整为 `145×28`、字号 `9F`，使其仅略大于二级 Tab；颜色和选中状态保持不变。

2026-09-20 项目版本命名更新：解决方案、项目目录、项目文件、程序集、产品标题和输出 EXE 统一增加日期后缀 `20260920`，名称为 `WJ_DSRCProtocolTest_Desk_Net_20260920`；C# 命名空间保持原值，避免影响现有类型引用。新增项目级 `.agents\skills\iteration-records\SKILL.md`，用于在用户触发“生成迭代记录”或“生成操作说明”时按确认模板生成项目文档；本次尚未创建迭代记录文件。

2026-09-20 新增 DOCX 操作说明 skill：新增 `.agents\skills\operation-manual-docx\SKILL.md`，用于在用户触发“生成操作说明”时按“一级产品 Tab→二级页面→区域/控件→最底层测试用例”组织 Word 文档，要求对按钮、输入框、选择框、日志区和测试用例说明日常操作、效果及判断条件，并通过页面截图、数字标注和控件说明表对应展示。该 skill 还规定 DOCX 渲染、逐页视觉检查、敏感信息脱敏和未实现功能边界；本次仅新增 skill，未生成 `操作说明.docx`。`iteration-records` skill 的职责已收窄为迭代记录。
